# 그래프에는 코드만 남긴다.
#
# 왜: 문서가 섞여 있으면 자연어 질의가 문서 쪽으로 빨려간다. 실제로
# "비콘 세트를 Firebase 에 업로드하는 경로" 를 물었더니 상위 결과가
# PROGRESS.md 와 .planning/** 이었고 코드 노드는 뒤로 밀렸다.
# 문서는 grep 으로 찾으면 되지만, 코드 호출 관계는 그래프가 아니면 못 본다.

*.md
*.txt
.planning/
graphify-out/
raw/
