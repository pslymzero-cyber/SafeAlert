<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="@dimen/sa_space_md" android:paddingEnd="@dimen/sa_space_md"
    android:paddingTop="@dimen/sa_space_md" android:paddingBottom="@dimen/sa_space_md"
    android:background="?attr/selectableItemBackground">

    <!-- 기기 정보 행 -->
    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="horizontal" android:gravity="center_vertical">
        <LinearLayout android:layout_width="0dp" android:layout_height="wrap_content"
            android:layout_weight="1" android:orientation="vertical">
            <TextView android:id="@+id/tv_device_name"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:textSize="@dimen/sa_type_value" android:textStyle="bold" android:textColor="@color/sa_text_primary"/>
            <!-- MAC 주소 -->
            <TextView android:id="@+id/tv_mac_addr"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/sa_space_hairline"
                android:textSize="@dimen/sa_type_caption" android:textColor="@color/sa_text_tertiary"
                android:fontFamily="monospace"/>
            <!-- UUID (있을 때만 표시) -->
            <TextView android:id="@+id/tv_uuid"
                android:layout_width="wrap_content" android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/sa_space_hairline"
                android:textSize="@dimen/sa_type_micro" android:textColor="@color/sa_text_tertiary"
                android:fontFamily="monospace"
                android:visibility="gone"/>
        </LinearLayout>
        <!-- RSSI -->
        <TextView android:id="@+id/tv_rssi"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:textSize="@dimen/sa_type_caption" android:textColor="@color/sa_text_tertiary"/>
    </LinearLayout>

    <!-- 등록 버튼 행 -->
    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="horizontal" android:layout_marginTop="@dimen/sa_space_sm">
        <Button android:id="@+id/btn_add_uuid"
            android:layout_width="wrap_content" android:layout_height="34dp"
            android:text="UUID 등록" android:textSize="@dimen/sa_type_caption"
            style="@style/Widget.MaterialComponents.Button.UnelevatedButton"
            android:backgroundTint="@color/sa_tint_blue" android:textColor="@color/sa_accent"
            android:visibility="gone"/>
        <View android:layout_width="8dp" android:layout_height="@dimen/sa_stroke_hairline"/>
        <Button android:id="@+id/btn_add_mac"
            android:layout_width="wrap_content" android:layout_height="34dp"
            android:text="MAC 등록" android:textSize="@dimen/sa_type_caption"
            style="@style/Widget.MaterialComponents.Button.UnelevatedButton"
            android:backgroundTint="@color/sa_tint_amber" android:textColor="@color/sa_orange"/>
    </LinearLayout>
</LinearLayout>
