---
phase: 02-golden
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - app/src/test/java/com/wf11/safealert/ble/LowSpeedApproachRegressionTest.kt
  - app/src/main/java/com/wf11/safealert/03_service/BleService.kt
  - .github/workflows/release.yml
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [BUG-02]
tags: [android, kotlin, robolectric, golden-test, regression, safety-critical, ci]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "저속 접근(약한 콜드스타트에서 프레임당 미세 상승) 시퀀스의 프레임별 판정 결과가 수정 전 동작으로 1회 기록되고 커밋된다 (D-3A 1단계)"
    - "BUG-02 의 근본 원인이 코드 실측으로 규명되고, PROJECT.md 의 기존 진단이 맞는지 틀리는지가 명시적으로 판정된다 (D-3B)"
    - "수정 후 저속 접근 시퀀스가 경고 등급에 도달하며, 도달 프레임이 수정 전보다 빠르거나(또는 수정 전에는 도달하지 않던 것이 도달하도록) 개선된다"
    - "수정 전후 골든 diff 가 BUG-02 수정의 실제 효과를 프레임 단위로 보여준다 (D-3A 3단계)"
    - "02-02·02-03 이 동결한 기존 골든은 BUG-02 수정 후에도 한 값도 바뀌지 않는다 (D-3D 수정 범위 게이트)"
    - "새로 추가된 골든 테스트 클래스가 CI 필수 리포트 목록에 등재되어, 클래스가 사라지거나 실행되지 않으면 릴리스가 차단된다 (CI-01 연계)"
    - "REQUIREMENTS.md TEST-02 에 '좀비 워치독' 용어가 현행 신선도 게이트로 해석된 근거가 각주로 남는다 (D-4A follow-up)"
    - "현장 확인 4항목(저속 접근 시 경고 표시·속도별 거리 비교·해제)이 REQUIREMENTS.md BUG-02 각주로 인계되어, 현장 환경이 확보되는 시점에 수행 가능한 절차로 남는다 (Phase 내 차단 게이트가 아니다)"
    - statement: "수정 전 기록 커밋에는 버그가 살아있는 동작이 기대값으로 담겨 있으며, 그 사실이 커밋 메시지에 명시된다"
      verification: backstop
    - statement: "수정 대상 판정 상수·분기의 변경 범위가 저속 접근 경로에 국소화되고, 공유 경로 변경이 불가피한 경우에만 예외로 사용자 판단을 거친다"
      verification: backstop
    - statement: "합성 시퀀스가 재현에 실패하면 기울기·시작값을 조정해 재현 하한을 탐색하고, 조정 결과가 기록된다"
      verification: backstop
  artifacts:
    - app/src/test/java/com/wf11/safealert/ble/LowSpeedApproachRegressionTest.kt
    - .github/workflows/release.yml
    - .planning/REQUIREMENTS.md
  key_links:
    - "LowSpeedApproachRegressionTest → BleServiceTestHarness(02-01) → processAlert 의 nowMs 시임"
    - "저속 골든 diff(수정 전 커밋 ↔ 수정 후 커밋) → BUG-02 수정 효과의 유일한 객관적 증거"
    - "02-02·02-03 골든 무변경 → BUG-02 수정 범위의 게이트 (D-3D)"
    - "release.yml 의 필수 리포트 클래스 목록 → TEST-com.wf11.safealert.ble.<클래스>.xml → 골든 미실행 시 릴리스 차단"
  prohibitions:
    - "판정 반경 값(지게차 15m/8m, 그 외 5m/3m)을 변경하지 않는다"
    - "UWB 를 제거하거나 비활성화하지 않는다"
    - "UWB 를 主 판정으로 승격하지 않는다"
    - "1바이트 비트팩 페이로드 레이아웃을 변경하지 않는다"
    - "신규 기능을 추가하지 않는다"
    - "app/src/main 에 테스트 전용 코드·리플렉션 훅을 남기지 않는다"
    - "기존 골든이 움직였을 때 사용자 판단 없이 재동결하지 않는다 (D-3D)"
    - "골든 자동 갱신 경로(updateGolden 류 옵션)를 만들지 않는다 (D-09 계승)"
    - "PROJECT.md 의 기존 진단을 확인 없이 사실로 전제하지 않는다 (D-3B)"
---

<user_story>
**As a** 현장 보행자
**I want to** 지게차가 천천히 다가올 때도 빠르게 다가올 때와 똑같이 경고를 받고
**so that** "느리니까 안 울린다"는 이유로 코앞까지 붙는 상황이 생기지 않는다.

출처: REQUIREMENTS.md BUG-02 + ROADMAP Phase 2 성공 기준 4번 (ROADMAP Goal 행은 사용자 스토리 형식이 아니므로 요구사항 원문에서 파생 — 창작하지 않음).
</user_story>

<objective>
저속 접근 미탐지(BUG-02)를 골든으로 포착하고, 근본 원인을 규명해 고치고, 수정 효과를 골든 diff 로 증명한다. 그리고 이번 Phase 가 만든 골든들이 CI 에서 실제로 실행되도록 필수 목록에 등재한다.

Purpose: BUG-02 는 현장에 나가 있는 미탐지다. 지금 고쳐야 Phase 3 의 REFACTOR-04(분해 후 기대값 불변 게이트)가 버그가 아니라 올바른 동작을 보존하게 된다. 그리고 이 수정은 이번 Phase 에서 유일하게 프로덕션 판정 동작을 실제로 바꾸는 작업이므로, 무엇이 왜 바뀌었는지가 골든 diff 로 남아야 한다.

Output: 저속 접근 회귀 테스트 1파일, BleService 판정 수정, CI 필수 목록 갱신, 요구사항 각주.
</objective>

<constraints>
- **D-3A 순서를 반드시 지킨다.** (1) 버그가 살아있는 상태로 저속 골든을 기록·커밋 → (2) 수정 → (3) 저속 골든 재동결. 순서를 바꾸면(먼저 고치고 나서 골든을 기록하면) diff 가 사라지고, 수정이 실제로 무엇을 바꿨는지 증명할 방법이 없어진다. 수용 비용은 중간 커밋 1개가 버그 동작을 기대값으로 담은 채 저장소에 남는 것이며, 이는 커밋 메시지로 표시한다.
- **D-3B: 근본 원인은 미확정이다.** PROJECT.md 에 적힌 진단(칼만 워밍업 주입이 프리셋 최소값에 포화된다는 설명)은 **가설로만 취급한다**. 02-CONTEXT.md 조사에서 `injectWarmup` 본체는 상수 3개만 세팅하고 프리셋을 참조하지도 클램프를 걸지도 않는 것으로 확인됐다 — 즉 기존 진단은 코드와 맞지 않는다. 원인 규명은 Task 1 이 만든 골든이 보여주는 실제 프레임 거동에서 출발한다. 가설을 확인 없이 코드에 반영하지 않는다.
- **D-3D: 기존 골든 불변 = 수정 범위 게이트.** 수정 후 02-02·02-03 골든이 한 값이라도 바뀌면 **재동결하지 말고 수정안을 좁힌다**. 탈출구는 하나뿐: 공유 경로(예: 칼만 초기 공분산처럼 모든 시나리오가 지나는 지점)라 국소화가 물리적으로 불가능한 경우. 그 경우에도 재동결 전에 사용자에게 무엇이 왜 함께 움직였는지 보고하고 판단을 받는다.
- **프로덕션 변경은 BleService.kt 판정 경로 한정.** `KalmanFilter.kt` 를 포함한 다른 프로덕션 파일은 이 계획의 `files_modified` 에 없다. 다른 파일을 고쳐야 한다는 결론이 나오면 임의로 확장하지 말고 정지하고 보고한다 — 그 자체가 D-3D 가 말하는 "공유 경로" 신호다.
- **패키지는 `com.wf11.safealert.ble`.** `release.yml` 의 리포트 경로가 이 접두사를 하드코딩한다.
- **`release.yml` 변경은 최소.** 필수 클래스 목록에 이번 Phase 의 새 클래스명 추가 + 최소 테스트 수 상향 + 하드코딩 접두사 제약을 알리는 주석 한 줄. 워크플로 구조를 리팩터링하지 않는다.
- **record-then-freeze 유지.** 자동 갱신 옵션을 만들지 않는다. 재동결은 사람이 값을 갈아 끼우는 명시적 행위여야 한다.
- **BLE 페이로드·판정 반경 불변.** 어떤 수정도 1바이트 비트팩 레이아웃이나 경고/위험 반경 값을 건드리지 않는다.
</constraints>

<artifacts_this_phase_produces>

| 심볼 / 항목 | 종류 | 역할 |
|------------|------|------|
| `LowSpeedApproachRegressionTest` | class | 저속 접근 회귀 골든 테스트 클래스 |
| `START_RSSI_DBM` | private const | 합성 시퀀스 시작 신호세기 (약한 콜드스타트) |
| `SLOPE_MILLI_DB_PER_FRAME` | private const | 프레임당 상승폭 (정수 밀리dBm — 부동소수 누적오차 회피) |
| `FRAMES` | private const | 시퀀스 프레임 수 |
| `T0_MS` | private val | 시각 앵커 |
| `rssiAtFrame(i)` | private fun | 프레임 번호 → 신호세기 (정수 산술) |
| `runLowSpeedApproach()` | private fun | 시퀀스 재생 후 프레임 시계열 문자열 반환 |
| `lowSpeedApproach_golden` | @Test | 프레임별 판정 시계열 동결 |
| `lowSpeedApproach_reachesWarning` | @Test | 경고 도달 성질 단언 (수정 후에만 통과) |
| release.yml `REQUIRED` 목록 | CI 설정 | 새 골든 클래스 3종 등재 |
| release.yml `MIN_TOTAL` | CI 설정 | 최소 테스트 수 상향 |
| REQUIREMENTS.md TEST-02 각주 | 문서 | '좀비 워치독' → 현행 신선도 게이트 해석 근거 |

</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-golden/02-CONTEXT.md
@.planning/phases/02-golden/02-01-SUMMARY.md
@.planning/phases/02-golden/02-02-SUMMARY.md
@.planning/phases/02-golden/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 저속 접근 골든 — 버그가 살아있는 동작으로 기록·커밋</name>

  <precondition>02-02 가 동결한 상승·해제 골든이 저장소에 있고 현재 통과한다 — 이 골든들이 D-3D 게이트의 기준선이므로, 없거나 실패하는 상태에서 BUG-02 수정에 착수하면 수정 범위를 판정할 수 없다.</precondition>

  <read_first>
    - `app/src/test/java/com/wf11/safealert/ble/AlertCascadeGoldenTest.kt` (02-02 산출물 — 시퀀스 재생·직렬화 형식 규율을 그대로 따르기 위해)
    - `app/src/test/java/com/wf11/safealert/ble/support/BleServiceTestHarness.kt` (제공 API)
    - `.planning/phases/02-golden/02-02-SUMMARY.md` (채택된 프레임 간격·시각 앵커·헤더 4요소 형식)
    - `app/src/main/java/com/wf11/safealert/03_service/BleService.kt` 1406-1470행 (프레임 진입·시각 취득·워밍업 주입 지점), 1560-1620행 (유효 임계 계산과 등급 판정 진입)
  </read_first>

  <behavior>
    - Behavior 1: `rssiAtFrame(i)` 가 정수 산술만으로 시작값에서 목표 종단값까지 단조 증가하며, 같은 입력에 대해 항상 같은 값을 낸다.
    - Behavior 2: 저속 접근 시퀀스를 재생하면 프레임별 관측면(신호세기, 필터 출력, 유효 임계, 등급, 두 접촉 streak, 게이트 통과 여부)이 결정적인 시계열 문자열로 나온다.
    - Behavior 3: 그 시계열이 기록된 기대값과 정확히 일치한다 (수정 전 동작 동결).
    - Behavior 4: 시퀀스 종단 신호세기가 경고 임계를 명백히 넘어섰음에도 등급이 경고에 도달하지 않는다 — 즉 BUG-02 가 이 골든에 실제로 포착된다. 포착되지 않으면 파라미터를 조정해 재탐색한다.
  </behavior>

  <files>app/src/test/java/com/wf11/safealert/ble/LowSpeedApproachRegressionTest.kt</files>

  <action>
D-3C 가 정한 합성 시퀀스를 인라인 상수로 만든다. 형태: 약한 콜드스타트 신호세기에서 시작해 프레임당 아주 작은 폭으로 단조 상승하는 시퀀스. 물리적 근거는 지게차가 저속으로 경고 반경에서 위험 반경까지 접근하는 시간이며, 프레임 간격은 02-02 가 채택한 값을 그대로 쓴다. 시작값·기울기·프레임 수를 파일 상단 상수 3개로 두고, 각 상수 옆에 어떤 물리 상황을 나타내는지 한 줄 주석을 단다.

기울기는 부동소수 누적을 피하려고 정수 밀리dBm 단위로 두고 `rssiAtFrame` 안에서 정수 나눗셈으로 dBm 을 만든다. 프레임 시계열 골든에서 부동소수 누적오차만큼 값이 흔들리면 골든이 플레이키해진다.

**핵심 판정: 이 시퀀스가 BUG-02 를 실제로 재현하는가.** 재생 후 종단 신호세기가 경고 임계를 넘겼는데도 등급이 경고에 도달하지 않으면 재현 성공이다. 첫 시도에서 재현되지 않으면 D-3C 의 Known gap 절차를 따른다: 기울기를 낮추거나 시작값을 더 약하게 잡아 재현 하한을 탐색한다. 탐색 과정에서 시도한 조합과 최종 채택값·그 근거를 SUMMARY 에 기록한다. **아무리 조정해도 재현되지 않으면 골든을 억지로 만들지 말고 정지하고 보고한다** — BUG-02 의 실제 트리거 조건이 이 합성 모델과 다르다는 뜻이고, 그 사실 자체가 Task 2 의 출발점이 되어야 한다.

관측면과 직렬화 형식은 02-02 를 그대로 따른다. 다만 이 골든은 **게이트 통과 여부**를 반드시 열에 포함한다 — BUG-02 가 "임계는 넘었는데 등급이 안 올라간다"는 증상이므로, 어디서 막히는지(streak 미달인지 게이트 차단인지)가 원인 규명의 1차 단서다. 이는 D-2E 가 관측면에 중간 판정 상태를 넣기로 한 이유와 같다.

기록이 끝나면 **이 태스크만 단독으로 커밋한다.** 커밋 메시지 본문에 이 골든이 수정 전(버그가 살아있는) 동작을 담고 있다는 사실과, 다음 커밋에서 재동결될 예정이라는 사실을 적는다. 나중에 저장소 이력을 보는 사람이 이 기대값을 올바른 동작으로 오해하면 안 된다.

Behavior 4 를 단언하는 테스트는 이 시점에 **통과**한다(버그가 살아있으므로). Task 2 가 수정한 뒤에는 이 단언이 뒤집혀야 하므로, 단언 방향이 바뀔 것임을 주석에 명시한다.
  </action>

  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "com.wf11.safealert.ble.LowSpeedApproachRegressionTest" && test 0 -eq $(git diff --name-only -- app/src/main | wc -l)</automated>
  </verify>

  <acceptance_criteria>
    - 저속 접근 시퀀스가 결정적으로 재생되고 프레임 시계열이 기대값과 일치한다.
    - 종단 신호세기가 경고 임계를 넘었음에도 등급이 경고에 도달하지 않음이 단언으로 포착된다 (BUG-02 재현).
    - 프레임 시계열에 게이트 통과 여부 열이 포함돼 있다.
    - 이 태스크가 단독 커밋이며, 커밋 메시지에 "버그 살아있는 동작 기록"이 명시돼 있다.
    - `app/src/main` 변경 0건.
    - 파라미터 탐색 과정과 최종 채택값이 SUMMARY 에 기록돼 있다.
  </acceptance_criteria>

  <reversibility rating="reversible">테스트 파일 1개 추가 + 단독 커밋. 되돌리기는 커밋 revert 1건.</reversibility>

  <done>BUG-02 가 실행 가능한 골든으로 포착되고, 수정 전 동작이 단독 커밋으로 저장소에 기록됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 근본 원인 규명 · 수정 · 저속 골든 재동결</name>

  <read_first>
    - Task 1 이 만든 `LowSpeedApproachRegressionTest.kt` 의 기록된 프레임 시계열 (원인 규명의 1차 증거)
    - `app/src/main/java/com/wf11/safealert/03_service/BleService.kt` — Task 1 시계열이 지목하는 구간. 최소한 프레임 진입·워밍업 주입 지점(1406-1470)과 등급 판정·게이트 구간(1560-1660)
    - `app/src/main/java/com/wf11/safealert/06_utils/KalmanFilter.kt` 130-160행 (`injectWarmup` 본체 — 기존 진단이 지목한 곳. **읽되 수정 대상은 아니다**)
    - `.planning/PROJECT.md` 의 BUG-02 진단 문구 (가설로만 취급)
  </read_first>

  <behavior>
    - Behavior 1: Task 1 시계열이 등급 상승을 막은 지점을 한 곳으로 좁혀 지목한다 (streak 미달 / 게이트 차단 / 유효 임계 계산 / 필터 출력 지연 중 어느 것인지).
    - Behavior 2: 수정 후 저속 접근 시퀀스가 경고 등급에 도달한다.
    - Behavior 3: 수정 후 저속 골든 시계열이 재기록되며, 수정 전 커밋과의 diff 가 어느 프레임부터 무엇이 달라졌는지 보여준다.
    - Behavior 4: 02-02 가 동결한 상승·해제 골든이 한 값도 바뀌지 않는다.
    - Behavior 5: 02-03 이 동결한 UWB 골든이 한 값도 바뀌지 않는다.
  </behavior>

  <files>app/src/test/java/com/wf11/safealert/ble/LowSpeedApproachRegressionTest.kt, app/src/main/java/com/wf11/safealert/03_service/BleService.kt</files>

  <action>
D-3B 에 따라 골든이 원인을 규명한다. Task 1 시계열에서 "종단 신호세기가 임계를 넘겼는데 등급이 안 올라간 프레임"을 잡고, 그 프레임의 각 열을 거꾸로 읽어 어디서 막혔는지를 좁힌다. 후보는 넷이다: 접촉 연속 카운터가 임계에 못 미쳤다 / 게이트가 차단했다 / 유효 임계 계산이 오프셋 때문에 너무 엄격했다 / 필터 출력이 원신호를 따라오지 못했다. 시계열의 열이 곧 이 넷을 구별하는 장치다.

**PROJECT.md 의 기존 진단을 전제로 삼지 않는다.** 02-CONTEXT.md 조사에서 그 진단이 지목한 함수는 상수만 세팅하고 프리셋을 참조하지 않는 것으로 확인됐다. 규명 결과가 기존 진단과 다르면, 무엇이 실제 원인이고 기존 진단이 왜 틀렸는지를 SUMMARY 에 적는다. 같더라도 어떤 증거로 확인했는지를 적는다. 이 기록이 없으면 다음 사람이 같은 가설을 다시 검증하게 된다.

**규명 결과 → 수정 지점 대응.** 어느 후보로 좁혀졌는지가 손댈 곳을 정한다. 후보마다 공유 경로 위험을 미리 못 박아 둔다 — 수정 착수 전에 "이 수정이 어느 골든을 건드릴 수 있는가"를 먼저 적고, 수정 후 실제 실행 결과와 대조한다. 예상과 실제가 어긋나면 그 사실 자체를 SUMMARY 에 적는다 — 공유 경로 오판이 D-3D 게이트를 무력화하는 유일한 경로다.

1. **접촉 연속 카운터(streak) 미달** — 시계열의 streak 열이 임계에 못 미친 채 구간이 끝났다면, `warningContactStreakMap` 계열의 증가·리셋 조건을 고친다. 저속 접근에서만 리셋이 걸리는 분기가 있는지부터 본다. 저속 조건 전용 분기라 국소화 가능성이 가장 높다.
2. **유효 임계 비교 실패** — streak 은 찼는데 등급이 안 올라갔다면 유효 임계 계산식과 비교 연산자를 본다. 오프셋 누적으로 임계가 과도하게 엄격해졌는지, 비교 방향·경계 포함 여부가 저속 구간에서만 어긋나는지를 확인하고 그 한 지점만 고친다. 같은 식을 다른 시나리오도 지나므로 02-02 동시 이동 위험이 중간이다.
3. **필터 출력 지연** — 종단 입력은 강해졌는데 필터 출력 열이 따라오지 못했다면 칼만 초기값(워밍업 주입값)과 프리셋 파라미터를 본다. **모든 시나리오가 지나는 공유 경로이므로 D-3D 예외가 발동할 가능성이 가장 높다.** 착수 전에 그 사실을 SUMMARY 초안에 미리 적고, `KalmanFilter.kt` 본체를 고쳐야 한다는 결론이 나오면 그 시점에 정지하고 보고한다 (이 계획의 `files_modified` 밖이다).
4. **게이트 차단** — 위 셋이 정상인데도 등급이 안 올라갔다면 등급 상승 앞단의 게이트 로직(판정 권위 분기·신선도 게이트 등)을 본다. 02-03 UWB 골든과 직접 겹치므로 02-03 동시 실행 결과가 판정의 핵심 근거다.

어느 후보든 수정안은 **한 지점 최소 폭**으로 제시한다. 여러 지점을 동시에 고쳐야 효과가 난다는 결론이 나오면 그 자체를 보고한다 — 원인이 아직 하나로 좁혀지지 않았다는 신호다.

수정은 **저속 접근 경로에 국소화**한다. 수정안을 적용한 직후 곧바로 02-02·02-03 골든을 함께 돌린다(Behavior 4·5) — 다른 작업을 끼워 넣지 않는다. 이것이 D-3D 수정 범위 게이트다:
- 기존 골든이 전부 그대로면 → 저속 골든을 재동결하고 진행한다.
- 기존 골든이 하나라도 움직이면 → **재동결하지 말고 수정안을 좁힌다.** 더 좁은 수정으로 같은 효과가 나는지 먼저 시도한다.
- 좁히기가 물리적으로 불가능한 공유 경로(모든 시나리오가 지나는 지점)라고 판단되면 → **정지하고 사용자에게 보고한다.** 무엇이 함께 움직였는지, 왜 국소화가 불가능한지, 함께 움직인 값이 개선인지 퇴행인지를 제시하고 판단을 받는다. 사용자 판단 없이 기존 골든을 재동결하지 않는다.

재동결은 값을 손으로 갈아 끼우는 명시적 행위로 한다. 자동 갱신 옵션을 만들지 않는다.

Task 1 이 남긴 "단언 방향이 뒤집힌다"는 주석대로, 경고 미도달을 단언하던 테스트를 경고 도달 단언으로 바꾼다. 도달 프레임 번호를 기대값에 포함해, 나중에 도달이 느려지는 회귀도 잡히게 한다.

수정 diff 는 최소로 유지한다. 이번 Phase 에서 프로덕션 판정 동작을 실제로 바꾸는 유일한 지점이며, 현장 기기에 나가는 변경이다. 리팩터링·정리·주변 개선을 같은 커밋에 섞지 않는다.

수정과 재동결은 한 커밋으로 묶는다 — 그래야 Task 1 커밋과의 diff 가 정확히 "BUG-02 수정의 효과"가 된다.
  </action>

  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests "com.wf11.safealert.ble.*" && git diff --stat HEAD~1 -- app/src/main/java/com/wf11/safealert/03_service/BleService.kt</automated>
  </verify>

  <acceptance_criteria>
    - 저속 접근 시퀀스가 경고 등급에 도달하고, 도달 프레임 번호가 기대값에 포함된다.
    - 수정 전 커밋과의 저속 골든 diff 가 프레임 단위로 수정 효과를 보여준다.
    - 02-02·02-03 골든이 한 값도 바뀌지 않았다 (또는 예외 발동 시 사용자 판단 기록이 SUMMARY 에 있다).
    - 프로덕션 변경이 `BleService.kt` 판정 경로에 한정되고, diff 에 리팩터링·정리가 섞이지 않았다.
    - SUMMARY 에 근본 원인과 그 증거, 그리고 기존 진단과의 일치/불일치 판정이 기록돼 있다.
    - 규명된 후보(1~4 중 어느 것인지)와 그에 대응해 실제로 손댄 지점, 착수 전 예상한 영향 골든과 실제 실행 결과의 대조가 SUMMARY 에 있다.
  </acceptance_criteria>

  <reversibility rating="costly">현장 기기의 경보 판정 동작을 실제로 바꾼다. 코드 되돌리기는 커밋 revert 1건으로 쉽지만, 잘못된 수정이 배포되면 경보 오발령 또는 미발령이 현장에 나가고 회수는 사용자의 현장 가용 시간에 묶인다. 완화: D-3D 범위 게이트 + 저속 골든 diff + Task 3 이 REQUIREMENTS.md 로 인계하는 출하 후 현장 확인 4항목.</reversibility>

  <done>BUG-02 의 근본 원인이 증거와 함께 규명·수정되고, 저속 골든이 재동결됐으며, 기존 골든이 전부 불변임이 확인돼 커밋됐다.</done>
</task>

<task type="auto">
  <name>Task 3: CI 필수 목록 등재 · 요구사항 각주 · 현장 확인 절차 인계</name>

  <read_first>
    - `.github/workflows/release.yml` 의 테스트 리포트 검증 스텝 전체 (필수 클래스 목록·최소 테스트 수·리포트 경로 조립부)
    - `.planning/REQUIREMENTS.md` 의 TEST-02 항목
    - `.planning/phases/02-golden/02-01-SUMMARY.md` · `02-02-SUMMARY.md` · `02-03-SUMMARY.md` (실제로 생긴 테스트 클래스명과 테스트 개수)
  </read_first>

  <files>.github/workflows/release.yml, .planning/REQUIREMENTS.md</files>

  <action>
`release.yml` 의 테스트 리포트 검증 스텝에 이번 Phase 가 만든 골든 클래스들을 필수 목록으로 등재한다. 클래스명은 상상하지 말고 위 세 SUMMARY 에서 실제 생성된 이름을 읽어 옮긴다. 최소 테스트 수 하한도 실제 개수에 맞춰 올린다 — 하한을 실제보다 낮게 두면 테스트가 조용히 사라져도 CI 가 통과한다.

리포트 경로 조립부가 패키지 접두사를 하드코딩하고 있다는 사실을 주석 한 줄로 남긴다. 이번 Phase 의 클래스들은 전부 그 접두사 아래에 두어 문제가 없지만, 다음 사람이 다른 패키지에 테스트를 만들면 리포트를 못 찾은 채 조용히 지나간다. 주석은 그 함정을 드러내는 용도이며, 워크플로 구조를 고치지 않는다 — 구조 개선이 필요하다고 판단되면 별도 항목으로 보고한다.

`REQUIREMENTS.md` TEST-02 에 각주를 단다: 요구사항 원문의 '좀비 워치독'이라는 이름의 심볼은 코드에 존재하지 않으며(v1.1.46 에서 세션 철거 수단이 폐지되고 신선도 게이트만 남음), 02-03 은 이를 현행 신선도 게이트 기준으로 해석해 (a) 표본 단절 시 판정 권위 강등 (b) 신선 실측 조회의 null 반환 (c) 낡은 근거리 실측의 좀비 경보 부재 세 항으로 골든화했다는 사실. D-4A 가 남긴 follow-up 이다. 요구사항 항목의 체크 상태는 이 태스크에서 바꾸지 않는다 — 완료 판정은 Phase 종료 절차의 몫이다.

마지막으로 현장 확인 절차를 **출하 후 관찰 항목으로 인계**한다. 이 Phase 의 성공 기준 중 하나는 저속으로 접근하는 지게차가 실제로 경고를 띄우는 것이고, 그것은 JVM 골든이 아니라 현장에서만 확인된다. 그러나 그 확인에는 지게차 1대 + 보행자 기기 + 저속 접근 재현이 가능한 현장 시간(반복 접근을 위한 연속 운용 포함)이 필요하고, 그 시간이 이 Phase 안에 확보된다는 보장은 어느 문서에도 없다 — 검증 사이클이 사용자의 현장 가용 시간에 묶이는 것은 이 프로젝트의 상수다. 그래서 현장 확인을 Phase 완료의 차단 게이트로 두지 않고, Phase 1 이 출하 상태를 다룬 방식 그대로 **APK 는 수정을 담아 나가고 현장 확인은 그 뒤의 관찰**로 잡는다 (02-CONTEXT.md 도메인 절의 출하 방침과 같다).

인계 방법: `REQUIREMENTS.md` BUG-02 항목에 각주로 4항목 체크리스트를 그대로 적는다 — 수정된 앱을 기기 2대(보행자 1 + 지게차 1)에 설치한 뒤 (1) 지게차가 걷는 속도보다 느리게 경고 반경 밖에서 안으로 접근, (2) 경고 반경 진입 전후로 보행자 기기에 경고가 표시됨, (3) 평소 속도 접근과 비교해 경고가 뜨는 거리가 크게 다르지 않음, (4) 지게차가 멀어지면 경보가 정상 해제됨. 그리고 이 4항목이 확인되기 전까지 BUG-02 는 "코드 수정 완료, 현장 관측 대기" 상태라는 것을 각주에 명시한다. 2번 항목이 실패로 보고되면 BUG-02 를 재개봉한다는 조건도 함께 적는다. BUG-02 의 체크 상태는 이 태스크에서 바꾸지 않는다.
  </action>

  <verify>
    <automated>test 0 -eq $(git diff --name-only -- app/src/main | wc -l) && ./gradlew :app:testDebugUnitTest --tests "com.wf11.safealert.ble.*"</automated>
    <human-check>
확인할 것은 현장 확인 4항목이 `REQUIREMENTS.md` BUG-02 각주로 남아, 현장 기회가 생기는 시점에 그대로 수행 가능한 상태인가다.

현장 환경(지게차 1대 + 보행자 기기 + 저속 접근 재현)이 **지금 확보돼 있다면** 그 4항목을 즉시 수행하고 결과(경고가 뜬 대략 거리, 속도별 차이, 해제 여부)를 보고한다. 2번 항목이 실패하면 BUG-02 수정이 현장에서 듣지 않은 것이므로 그대로 보고하고 BUG-02 를 재개봉한다.

확보돼 있지 않다면 "현장 미수행 — 출하 후 관찰 대기"로 표기하고 넘어간다. **이 항목은 Phase 완료를 차단하지 않는다.**
    </human-check>
  </verify>

  <acceptance_criteria>
    - `release.yml` 필수 클래스 목록에 이번 Phase 의 골든 클래스가 전부 등재됐다.
    - 최소 테스트 수 하한이 실제 개수에 맞게 상향됐다.
    - 패키지 접두사 하드코딩 제약을 알리는 주석이 한 줄 추가됐다.
    - `REQUIREMENTS.md` TEST-02 에 '좀비 워치독' 해석 각주가 추가됐다.
    - `app/src/main` 변경 0건 (이 태스크는 프로덕션 코드를 건드리지 않는다).
    - `REQUIREMENTS.md` BUG-02 에 현장 확인 4항목 체크리스트와 "현장 관측 대기" 상태·재개봉 조건이 각주로 추가됐다.
    - 현장 확인 수행 여부(수행 시 결과 / 미수행 시 '출하 후 관찰 대기')가 SUMMARY 에 명시됐다.
  </acceptance_criteria>

  <reversibility rating="reversible">CI 설정과 문서 변경. 되돌리기는 커밋 revert 1건.</reversibility>

  <done>이번 Phase 의 골든이 CI 필수 게이트에 등재되고, TEST-02 해석 근거가 요구사항에 남았으며, 저속 접근 경고의 현장 확인 4항목이 REQUIREMENTS.md BUG-02 각주로 인계돼 출하 후 수행 가능한 상태.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 판정 로직 변경 → 현장 기기 | 이번 Phase 에서 유일하게 실제 경보 동작을 바꾸는 경계다. 잘못 넘으면 오발령 또는 미발령이 현장에 나간다. |
| 골든 기대값 → 회귀 판정 권위 | 기대값이 잘못 재동결되면 버그가 스펙으로 승격되고, 이후 모든 회귀 검사가 그 잘못된 기준을 지킨다. |
| CI 필수 목록 → 릴리스 차단 권한 | 목록이 실제 클래스와 어긋나면 테스트가 사라져도 릴리스가 통과한다. |
| 저장소 이력 → 다음 유지보수자 | Task 1 커밋은 의도적으로 버그 동작을 담는다. 표시가 없으면 오해된다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16 | Tampering | BUG-02 수정이 저속 경로 밖 판정까지 바꿈 | high | mitigate | D-3D 범위 게이트 — 02-02·02-03 골든 무변경이 수정 수용 조건. 움직이면 수정안을 좁히고, 공유 경로일 때만 사용자 판단 |
| T-02-17 | Repudiation | 재동결된 기대값이 왜 그 값인지 사후 불명 | medium | mitigate | D-3A 순서 강제 — 수정 전 단독 커밋과 수정+재동결 커밋의 diff 가 곧 수정 효과의 기록. 커밋 메시지에 버그 동작 기록임을 명시 |
| T-02-18 | Elevation of Privilege | 미검증 가설(PROJECT.md 기존 진단)이 확인 없이 코드에 반영 | high | mitigate | D-3B — 원인은 골든 시계열이 규명. 기존 진단은 가설로만 취급하고 일치/불일치를 SUMMARY 에 명시 판정 |
| T-02-19 | Denial of Service | CI 필수 목록 누락으로 골든이 조용히 미실행 | medium | mitigate | Task 3 이 실제 클래스명·개수를 SUMMARY 에서 읽어 등재하고 하한을 실제치로 상향. 패키지 접두사 하드코딩 함정을 주석으로 노출 |
| T-02-20 | Spoofing | 합성 시퀀스가 실제 저속 접근을 대표하지 못해, 고쳤다고 믿지만 현장은 그대로 | high | mitigate | 합성 시퀀스 파라미터와 대표성 근거를 SUMMARY 에 기록해 현장 결과와 대조 가능하게 남기고, Task 3 이 현장 확인 4항목을 REQUIREMENTS.md BUG-02 각주로 인계해 출하 후 종단 확인 경로를 확보. 2번 항목 실패 보고 시 BUG-02 재개봉 (현장 시간이 Phase 안에 확보된다는 보장이 없으므로 Phase 완료 차단 게이트로는 두지 않는다) |
| T-02-21 | Information Disclosure | CI 테스트 리포트에 현장 로그 반입 | low | accept | 시퀀스가 전부 인라인 합성 상수이고 현장 캡처를 리소스로 들이지 않으므로 잔여 위험이 무시할 수준 |
</threat_model>

<verification>
- `./gradlew :app:testDebugUnitTest --tests "com.wf11.safealert.ble.*"` 전체가 통과한다.
- Task 1 커밋과 Task 2 커밋 사이의 저속 골든 diff 가 존재하며 프레임 단위로 읽힌다.
- Task 2 이후 02-02·02-03 골든 파일에 기대값 변경이 없다 (`git diff` 로 확인).
- 프로덕션 diff 가 `BleService.kt` 한 파일에 한정된다.
- `release.yml` 필수 클래스 목록과 실제 테스트 클래스 집합이 일치한다.
- 현장 확인 4항목이 REQUIREMENTS.md BUG-02 각주로 인계됐고, 수행 여부(수행 시 결과 / 미수행 시 '출하 후 관찰 대기')가 보고됐다.
</verification>

<success_criteria>
- BUG-02 가 골든으로 포착되고, 원인이 증거와 함께 규명되고, 수정 효과가 diff 로 증명됐다.
- 수정 범위가 저속 접근 경로에 국소화됐고, 기존 골든이 전부 불변이다.
- 이번 Phase 의 골든 전부가 CI 필수 게이트에 걸려, 사라지거나 실행되지 않으면 릴리스가 차단된다.
- 저속 접근 경고의 현장 확인 4항목이 출하 후 관찰 항목으로 문서에 인계됐다 — 현장 환경이 확보되는 시점에 그대로 수행 가능한 상태다.
</success_criteria>

<output>
Create `.planning/phases/02-golden/02-04-SUMMARY.md` when done.

SUMMARY 에 반드시 포함할 것:
- 합성 시퀀스 파라미터 탐색 과정과 최종 채택값·근거
- BUG-02 근본 원인과 그 증거 (어느 프레임의 어느 열이 지목했는지)
- PROJECT.md 기존 진단과의 일치/불일치 명시 판정
- 수정 diff 요약과 국소화 근거
- D-3D 게이트 결과 (기존 골든 전부 불변 / 예외 발동 시 사용자 판단 내용)
- 저속 골든 수정 전↔후 diff 요약 (도달 프레임 변화)
- release.yml 등재 클래스 목록과 상향된 하한
- 현장 확인 4항목의 인계 위치(REQUIREMENTS.md BUG-02 각주)와 수행 여부 (수행 시 결과, 미수행 시 '출하 후 관찰 대기')
</output>
