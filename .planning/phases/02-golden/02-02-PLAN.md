---
phase: 02-golden
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - app/src/test/java/com/wf11/safealert/ble/AlertCascadeGoldenTest.kt
autonomous: true
requirements: [TEST-01]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "고정 RSSI 시퀀스를 재생하면 SAFE→WARNING→DANGER 격상의 등급과 전이 프레임이 기대값과 프레임 단위로 일치한다 (TEST-01, ROADMAP 성공기준 1)"
    - "역방향(멀어짐) 시퀀스를 재생하면 DANGER→WARNING→SAFE 해제의 등급과 전이 프레임이 기대값과 프레임 단위로 일치한다 (TEST-01)"
    - "판정 상수를 하나 건드리면 골든 테스트가 실패하고, 실패 메시지로 어떤 프레임의 무엇이 어떻게 달라졌는지 실기 없이 판별할 수 있다 (ROADMAP 성공기준 1, CI-02 선례)"
    - "alertState 진입시각은 t0 기준 상대 밀리초로 기록되어 실행 시각과 무관하게 재현된다 (D-2G)"
    - "매 프레임의 관측면 전체가 기록된다 — 종단 상태나 전이 프레임만 기록하지 않는다 (D-2F)"
    - statement: "등급 전이가 한 번도 없는 시퀀스에서는 BROADCAST_ALERT 브로드캐스트가 기대 건수(0 또는 기록된 값)와 정확히 일치한다"
      verification: backstop
    - statement: "동일 시퀀스를 두 번 재생하면 두 번의 기록이 완전히 동일하다 (판정에 실행 시각·난수 의존이 없다)"
      verification: backstop
    - statement: "칼만 워밍업이 끝나기 전 프레임(1~3)의 등급도 기대값으로 고정되어, 워밍업 구간 변경이 회귀로 잡힌다"
      verification: backstop
  artifacts:
    - "app/src/test/java/com/wf11/safealert/ble/AlertCascadeGoldenTest.kt — 격상 골든 / 해제 골든 / 재현성 테스트"
  key_links:
    - "BleServiceTestHarness.callProcessAlert(nowMs = { t0 + frame * FRAME_DT_MS }) → 프레임 단위 결정적 시각"
    - "renderFrame(service, deviceId, t0) → 프레임 1건을 정규 문자열 1줄로 직렬화 (골든 비교 단위)"
    - "kalmanFilters[deviceId].estimatedVel → 별도 DoubleArray 골든, 델타 1e-9 (문자열 골든과 분리)"
  prohibitions:
    - "판정 반경 값(지게차 15m/8m, 그 외 5m/3m) 변경 금지"
    - "UWB 제거·비활성화 금지"
    - "iBeacon 스캔 제거 금지"
    - "Firebase 프로필 공유 제거 금지"
    - "UWB 를 主 판정으로 승격 금지"
    - "신규 기능 추가 금지"
    - "1바이트 비트팩 페이로드 레이아웃 변경 금지"
    - "기대값 자동 갱신 경로 생성 금지 (수동 재동결만)"
    - "app/src/main 에 리플렉션·테스트 전용 코드 유입 금지"
    - "processAlert 로직 이동·추출 금지 (Phase 3 REFACTOR-01 범위)"
    - "medianValue·avgRssi 를 관측면에 추가 금지 (Phase 1 RssiCascadeTest 와 중복 — deferred)"
    - "DevSettings 기본값 드리프트 감시 테스트 추가 금지 (deferred)"
---

> **As a** SafeAlert 유지보수자,
> **I want to** 고정 RSSI 시퀀스를 재생해 SAFE→WARNING→DANGER 격상과 역방향 해제의 등급·전이 시점을 기대값과 비교하고,
> **so that** `BleService` 를 분해할 때 경보가 뜨는 시점과 꺼지는 시점이 1프레임이라도 밀리면 CI 가 먼저 잡아낸다.
>
> 출처: REQUIREMENTS.md TEST-01 원문.

<objective>
02-01 이 뚫어놓은 종단 슬라이스를 TEST-01 의 전체 폭으로 넓힌다. 한 프레임짜리 스모크를 **매 프레임 전체 시계열 골든** 두 벌(격상 / 해제)로 확장하고, 그 골든이 실제로 회귀를 잡는다는 것을 레드 트라이얼로 증명한다.

Purpose: 분해(Phase 3)의 수용 게이트인 REFACTOR-04 "기대값 불변" 이 성립하려면, 먼저 그 기대값이 존재하고 민감해야 한다. 민감하지 않은 골든은 안전망이 아니라 안전망처럼 보이는 것이다.
Output: `AlertCascadeGoldenTest` 의 격상 골든·해제 골든·재현성 테스트, 그리고 레드 트라이얼 증거.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-golden/02-CONTEXT.md
@.planning/phases/02-golden/02-01-SUMMARY.md

참조만:
- `app/src/test/java/com/wf11/safealert/support/BleServiceTestHarness.kt` (02-01 산출물 — 이 플랜은 수정하지 않고 사용만 한다)
- `app/src/test/java/com/wf11/safealert/ble/RssiCascadeTest.kt` (Phase 1 골든 형식 원형)
</context>

<constraints>
- **`BleServiceTestHarness.kt` 를 수정하지 않는다.** 이 플랜과 같은 웨이브의 02-03 이 같은 파일을 건드리면 병렬 실행이 깨진다. 격상/해제 골든에만 필요한 헬퍼는 `AlertCascadeGoldenTest.kt` 안에 `private` 로 둔다.
- **record-then-freeze (Phase 1 D-09 승계):** 기대값은 현재 동작을 1회 기록해 파일에 붙박는다. 자동 갱신 플래그·스크립트·환경변수 경로를 만들지 않는다. 재동결은 사람이 파일을 직접 편집하는 것만 허용된다.
- **관측면은 D-2E 로 고정:** 판정 결과 2종(`alertState` Pair, `BROADCAST_ALERT` 인텐트) + 중간 판정 상태 5종(`trackingStateMap`, `dangerContactStreakMap`, `warningContactStreakMap`, `fastApproachStreakMap`, `kalmanFilters[id].estimatedVel`). `medianValue`·`avgRssi` 는 추가하지 않는다.
- **경로 정정:** 02-CONTEXT.md `canonical_refs` 의 경로 접두사는 틀렸고 행 번호는 맞다. 실제 경로는 `03_service/BleService.kt`, `02_ble/KalmanFilter.kt`, `06_utils/UwbRanger.kt`.
</constraints>

<artifacts_this_phase_produces>
| 심볼 | 위치 | 종류 |
|------|------|------|
| `AlertCascadeGoldenTest.FRAME_DT_MS` | `test/.../ble/AlertCascadeGoldenTest.kt` | 프레임 간격 상수 (120L) |
| `AlertCascadeGoldenTest.T0_MS` | 동상 | 고정 기준 시각 상수 |
| `AlertCascadeGoldenTest.DEVICE_ID` | 동상 | 합성 MAC 상수 |
| `private fun renderFrame(...)` | 동상 | 프레임 1건 → 정규 문자열 1줄 직렬화 |
| `private fun runSequence(rssiSeq): Pair<List<String>, DoubleArray>` | 동상 | 시퀀스 재생 + 프레임별 기록 수집 |
| `private val ESCALATION_RSSI` / `RELEASE_RSSI` | 동상 | 입력 RSSI 시퀀스 (합성 상수에서 생성) |
| `private val ESCALATION_GOLDEN` / `RELEASE_GOLDEN` | 동상 | 프레임별 기대 문자열 인라인 리터럴 배열 |
| `private val ESCALATION_KFVEL` / `RELEASE_KFVEL` | 동상 | 프레임별 기대 kfVel 배열 (델타 1e-9) |
| `@Test escalation_goldenTimeline` | 동상 | 격상 골든 |
| `@Test release_goldenTimeline` | 동상 | 해제 골든 |
| `@Test sameSequence_replaysIdentically` | 동상 | 재현성 (backstop truth 검증) |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 격상 골든 — SAFE→WARNING→DANGER 매 프레임 시계열 기록·동결</name>
  <read_first>
    - `app/src/test/java/com/wf11/safealert/support/BleServiceTestHarness.kt` 전체 (02-01 산출물, 소형)
    - `app/src/test/java/com/wf11/safealert/ble/RssiCascadeTest.kt` :1-40 (헤더 주석 규약)
    - `app/src/main/java/com/wf11/safealert/03_service/BleService.kt` :380-390 (`alertState`), :640-690 (streak/state 맵 선언부)
    - `.planning/phases/02-golden/02-CONTEXT.md` — D-2E, D-2F, D-2G 항목 전문
  </read_first>
  <files>app/src/test/java/com/wf11/safealert/ble/AlertCascadeGoldenTest.kt</files>
  <behavior>
    - 시퀀스 재생: 프레임 i 의 시각은 `T0_MS + i * FRAME_DT_MS` 이며 `nowMs` 람다로 주입된다. `FRAME_DT_MS = 120L`.
    - 기록 단위: 프레임마다 `renderFrame` 이 (프레임번호, 입력 RSSI, `alertState` 레벨, `alertState` 진입시각 상대값, `trackingStateMap`, `dangerContactStreakMap`, `warningContactStreakMap`, `fastApproachStreakMap`, 누적 브로드캐스트 건수)를 고정폭 문자열 1줄로 직렬화한다.
    - 진입시각은 `entryMs - T0_MS` 로 기록하고, `alertState` 엔트리가 없으면 `null` 로 기록한다 (D-2G).
    - `kfVel` 은 문자열에 넣지 않고 별도 `DoubleArray` 로 수집해 델타 1e-9 로 비교한다 (D-2E).
    - 골든 비교는 전체 타임라인을 개행으로 이어 붙여 한 번에 비교한다 — 실패 시 어느 프레임이 어떻게 달라졌는지 diff 로 드러나야 한다 (CI-02 승계).
    - 시계열은 SAFE 구간 → WARNING 구간 → DANGER 구간을 순서대로 포함한다.
  </behavior>
  <action>
    `AlertCascadeGoldenTest.kt` 를 02-01 의 스모크 테스트 위에 확장한다. `@RunWith(RobolectricTestRunner::class)` 유지.

    **입력 시퀀스** — 상수 3개(`START_DBM`, `STEP_DBM`, `FRAMES`)에서 생성한다. 시작 제안값: `START_DBM = -95`, `STEP_DBM = +1`, `FRAMES = 42` (→ -95 dBm 에서 -54 dBm 까지 단조 접근, 42프레임 × 120ms ≈ 5.0초). 프레임별 입력 RSSI 는 계산식으로 만들되, 계산 결과를 `<verify>` 실행 전에 한 번 출력해 실제로 세 등급을 모두 통과하는지 확인한다.

    **동결 전 조정 허용 (중요):** 기록해 보니 시계열이 WARNING 이나 DANGER 에 도달하지 않거나, DANGER 도달이 마지막 1~2프레임에 몰려 전이 관찰이 불가능하면 `START_DBM`/`STEP_DBM`/`FRAMES` 를 조정해 **다시 기록한다**. 이 조정은 동결 이전에만 허용된다 — 일단 골든을 커밋한 뒤에는 입력 시퀀스도 기대값도 사람의 명시 지시 없이 바꾸지 않는다. 최종 채택 파라미터와 조정 사유를 헤더 주석과 SUMMARY 에 남긴다.

    **기대값** — 계산식이나 헬퍼로 만들지 않는다. 실제 1회 실행 결과를 **프레임당 한 줄의 인라인 리터럴**로 `ESCALATION_GOLDEN` 배열에 붙여 넣는다. `ESCALATION_KFVEL` 도 동일하게 실측값을 인라인 리터럴 배열로 붙인다. 기대값을 프로덕션 코드에서 다시 계산하면 골든이 아니라 동어반복이 된다.

    **헤더 주석** — `RssiCascadeTest.kt` 의 헤더 규약을 승계하되 다음을 반드시 포함한다: 기록 시점의 앱 버전(`versionName`), 기록 커밋 SHA, 기록 일시, 채택된 입력 파라미터 3개, 재동결 절차는 사람이 이 파일을 직접 편집하는 것뿐이라는 명시.

    **테스트 격리** — 각 `@Test` 는 `BleServiceTestHarness.newService()` 로 새 인스턴스를 얻고, 시작 전 `resetBetweenTests(service)` 를 호출한다. 상태맵이 인스턴스 필드이므로 인스턴스를 새로 만들면 격리되지만, 브로드캐스트는 애플리케이션 섀도우에 누적되므로 클리어가 필요하다.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*.AlertCascadeGoldenTest"</automated>
    <automated>./gradlew testDebugUnitTest</automated>
  </verify>
  <acceptance_criteria>
    - `escalation_goldenTimeline` 이 통과하고, `ESCALATION_GOLDEN` 의 원소 수가 `FRAMES` 와 같다
    - 골든 시계열에 SAFE·WARNING·DANGER 세 등급이 모두 등장하고, 등급이 단조 상승한다(중간에 내려갔다 오르는 구간이 있으면 그 사실 자체가 기록되고 헤더 주석에 언급된다)
    - `alertState` 진입시각이 `T0_MS` 상대값으로 기록되어 있고, 엔트리 부재 프레임은 `null` 로 기록되어 있다
    - `ESCALATION_KFVEL` 비교가 델타 1e-9 로 이뤄진다
    - 헤더 주석에 앱 버전·커밋 SHA·기록 일시·입력 파라미터 3개가 있다
    - 기존 골든 17건 + 02-01 스모크가 전부 통과한다
  </acceptance_criteria>
  <done>SAFE→WARNING→DANGER 격상 전 구간이 프레임 단위로 동결되어, 등급이 바뀌는 시점이 1프레임만 밀려도 테스트가 깨진다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 해제 골든 — DANGER→WARNING→SAFE 역방향 매 프레임 기록·동결</name>
  <read_first>
    - `app/src/test/java/com/wf11/safealert/ble/AlertCascadeGoldenTest.kt` (Task 1 산출물)
    - `app/src/main/java/com/wf11/safealert/03_service/BleService.kt` :2080-2320 (SAFE 2092/2229, DANGER 2299 브로드캐스트 지점 — 해제 경로의 판정 분기)
  </read_first>
  <files>app/src/test/java/com/wf11/safealert/ble/AlertCascadeGoldenTest.kt</files>
  <behavior>
    - 해제 시퀀스는 **격상 시퀀스를 먼저 재생해 DANGER 에 진입시킨 뒤**, 같은 서비스 인스턴스에서 이어서 멀어지는 램프를 재생한다. 상태 이력(streak, 칼만 속도, 진입시각)이 살아 있어야 해제 판정이 실제 경로를 탄다.
    - 시각은 격상 구간에서 이어서 증가한다 — 해제 프레임 j 의 시각은 `T0_MS + (FRAMES + j) * FRAME_DT_MS`.
    - 기록 형식은 Task 1 과 동일한 `renderFrame` 을 재사용한다.
    - 시계열은 DANGER 구간 → WARNING 구간 → SAFE 구간을 순서대로 포함한다.
  </behavior>
  <action>
    `RELEASE_RSSI` 를 격상 램프의 역방향으로 만든다. 시작 제안값: 격상 종단값에서 `-1` dBm/프레임으로 `RELEASE_FRAMES = 48`. 해제가 격상보다 느린 것이 정상이므로(해제 쪽 히스테리시스·streak 요구), 프레임 수를 격상보다 넉넉히 잡는다.

    Task 1 과 동일한 **동결 전 조정 허용** 규칙이 적용된다. 마지막 프레임까지 SAFE 로 내려오지 못하면 `RELEASE_FRAMES` 를 늘려 다시 기록한다. 단, 40프레임 이상 늘려도 SAFE 로 내려오지 않으면 **조정을 멈추고 그 사실을 사용자에게 보고한다** — 그것은 시퀀스 문제가 아니라 "경보가 꺼지지 않는" 실제 동작일 수 있고, 그런 동작을 골든으로 승격시키는 것은 버그를 스펙으로 만드는 일이다 (REQUIREMENTS.md BUG-03 배제 근거와 같은 논리).

    `RELEASE_GOLDEN` / `RELEASE_KFVEL` 도 실측 1회 결과를 인라인 리터럴로 붙인다.

    등급 해제 시 발생하는 `BROADCAST_ALERT`(SAFE 경로 2092/2229) 가 누적 브로드캐스트 건수에 반영되므로, 격상 구간에서 누적된 건수 위에서 이어 세어 기록한다.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*.AlertCascadeGoldenTest"</automated>
    <automated>./gradlew testDebugUnitTest</automated>
  </verify>
  <acceptance_criteria>
    - `release_goldenTimeline` 이 통과하고, `RELEASE_GOLDEN` 의 원소 수가 `RELEASE_FRAMES` 와 같다
    - 골든 시계열이 DANGER 에서 시작해 SAFE 로 끝난다 (WARNING 경유 여부는 실측대로 기록하되, 경유하지 않았다면 헤더 주석에 그 사실을 명시)
    - 해제 구간의 브로드캐스트 누적 건수가 격상 구간에서 이어진다
    - `sameSequence_replaysIdentically` 가 통과한다 — 격상+해제 전체를 두 번 재생한 결과가 완전히 동일하다
    - 기존 골든 전부 통과
  </acceptance_criteria>
  <done>격상과 해제 양방향 전 구간이 프레임 단위로 동결되었고, 같은 입력이 항상 같은 시계열을 낸다는 것이 테스트로 보장된다.</done>
</task>

<task type="auto">
  <name>Task 3: 레드 트라이얼 — 판정 상수를 건드리면 골든이 실제로 깨지는지 실증</name>
  <precondition>`git status --porcelain -- app/` 결과가 이 태스크 착수 시점의 베이스라인과 일치한다(Phase 1 선례대로 baseline-porcelain-equivalence 로 해석). 착수 전 이 출력을 SUMMARY 에 기록해 두고, 태스크 종료 시 같은 출력으로 돌아왔는지 대조한다.</precondition>
  <read_first>
    - `.planning/phases/01-ci/01-02-SUMMARY.md` — Phase 1 레드 트라이얼(Kalman 1e-6 / Median +1) 수행 방식과 4요소 실패 메시지 규약
    - `app/src/main/java/com/wf11/safealert/03_service/BleService.kt` :1406-1520 (섭동 대상 후보가 있는 구간)
  </read_first>
  <files>app/src/test/java/com/wf11/safealert/ble/AlertCascadeGoldenTest.kt</files>
  <action>
    골든이 민감한지 실증한다. 이 태스크는 **커밋되는 코드 변경을 남기지 않는다** — 프로덕션 섭동은 일시적이며 반드시 원복된다.

    절차:
    1. 착수 전 `git status --porcelain -- app/` 을 기록한다 (precondition 베이스라인).
    2. `03_service/BleService.kt` 의 판정에 직접 관여하는 상수/임계 **1건**을 최소 폭으로 섭동한다. 후보(택 1): 칼만 프리셋 파라미터, WARNING 임계 비교식의 상수, streak 요구 횟수. 섭동 폭은 "현장에서 사람이 눈치채기 어려운 정도" 여야 한다 — Phase 1 이 1e-6 / +1 을 쓴 이유가 그것이다.
    3. `./gradlew testDebugUnitTest --tests "*.AlertCascadeGoldenTest"` 를 돌려 **non-zero exit** 을 확인한다.
    4. 실패 메시지가 Phase 1 의 4요소 규약(어느 테스트 / 어느 프레임 / 기대값 / 실제값)을 만족하는지 확인한다. 만족하지 않으면 `renderFrame` 의 직렬화 포맷이나 assert 메시지를 고쳐 만족시킨다 — 이것이 CI-02 "실기 없이 판별 가능" 의 실체다.
    5. 섭동을 원복한다 (`git checkout -- app/src/main/java/com/wf11/safealert/03_service/BleService.kt` 또는 역편집).
    6. 재실행해 **zero exit** 으로 돌아오는지 확인한다.
    7. `git status --porcelain -- app/` 이 1번의 베이스라인과 일치하는지 확인한다.

    3~4번의 콘솔 출력(실패 메시지 발췌)과 6번의 성공 출력을 SUMMARY 에 증거로 인용한다. 섭동한 상수명·원래값·섭동값도 기록한다.

    골든이 **깨지지 않으면** 그것은 골든이 그 상수를 덮지 못한다는 뜻이다. 다른 상수로 재시도하지 말고 **왜 안 깨졌는지 원인을 규명해 사용자에게 보고한다** — 시퀀스가 그 상수의 영향권을 지나가지 않는 것이라면 시퀀스를 넓혀야 하고(동결 전 조정 규칙 적용), 관측면이 그 효과를 못 보는 것이라면 D-2E 의 관측면 자체를 재검토해야 한다.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest</automated>
    <automated>git diff --exit-code -- app/src/main</automated>
  </verify>
  <acceptance_criteria>
    - 섭동 상태에서 `AlertCascadeGoldenTest` 가 non-zero exit 으로 실패한 콘솔 증거가 SUMMARY 에 인용되어 있다
    - 실패 메시지가 4요소(테스트 / 프레임 / 기대값 / 실제값)를 모두 담고 있다
    - 원복 후 전체 스위트가 zero exit 으로 통과한다
    - `git diff --exit-code -- app/src/main` 이 변경 없음을 보고한다 (섭동 잔재 0)
    - 섭동한 상수명·원래값·섭동값이 SUMMARY 에 기록되어 있다
  </acceptance_criteria>
  <done>ROADMAP 성공기준 1 의 후반부("판정 상수를 건드리면 테스트가 실패한다")가 실증되었고, 프로덕션 코드에는 아무 흔적도 남지 않았다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 테스트 기대값 → 향후 리팩터의 정답 정의 | 이 파일의 리터럴이 Phase 3 REFACTOR-04 의 판정 기준이 된다. 여기 잘못 기록된 값은 버그를 스펙으로 승격시킨다 |
| 레드 트라이얼의 일시 섭동 → 프로덕션 코드 | 섭동이 원복되지 않으면 안전 판정 상수가 조용히 바뀐 채로 출하된다 |
| CI 실행 → 공개 아티팩트 | 골든 시계열이 테스트 리포트에 그대로 남는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | 골든 기대값 자동 갱신 경로 | high | mitigate | 자동 갱신 플래그·스크립트·환경변수 경로를 만들지 않는다(Phase 1 D-09 승계). 재동결은 사람이 파일을 직접 편집해 커밋하는 것만 허용. `must_haves.prohibitions` 에 명시 |
| T-02-06 | Tampering | 레드 트라이얼 섭동 잔재 | high | mitigate | Task 3 `<verify>` 의 `git diff --exit-code -- app/src/main` 을 통과 조건으로 강제. 착수 전/후 `git status --porcelain` 대조도 수용 기준에 포함 |
| T-02-07 | Repudiation | 기대값의 출처 불명 | medium | mitigate | 헤더 주석에 기록 시점의 앱 버전·커밋 SHA·기록 일시·입력 파라미터를 명기해, 나중에 "이 값이 어느 시점 동작인가" 를 되짚을 수 있게 한다 |
| T-02-08 | Information Disclosure | 골든 시계열이 담긴 CI 리포트 | low | mitigate | 입력은 합성 램프 상수, deviceId 는 합성 MAC 만 사용. 현장 로그·실측 시퀀스 미사용 |
| T-02-09 | Denial of Service | 시계열 골든의 부서지기 쉬움(과민 실패) | low | accept | 프레임 단위 동결은 의도된 민감도다(TEST-01 의 요구). 부동소수 비교만 델타 1e-9 로 완충 |
</threat_model>

<verification>
1. `./gradlew testDebugUnitTest` 전체가 통과한다.
2. `AlertCascadeGoldenTest` 에 격상 골든·해제 골든·재현성 테스트가 존재하고 모두 통과한다.
3. 레드 트라이얼의 실패 증거와 원복 증거가 SUMMARY 에 있다.
4. `git diff --exit-code -- app/src/main` 이 변경 없음이다 (이 플랜은 프로덕션 코드를 남기지 않는다).
5. `BleServiceTestHarness.kt` 가 이 플랜에서 수정되지 않았다 (02-03 과의 병렬 안전성).
</verification>

<success_criteria>
- TEST-01 의 격상·해제 양방향이 프레임 단위 기대값으로 고정되었다
- 그 기대값이 판정 상수 섭동에 민감하다는 것이 실증되었다
- 실패 시 실기 없이 원인을 판별할 수 있는 메시지 형식이 확립되었다
- 같은 입력이 항상 같은 결과를 낸다 (PROJECT.md 의 핵심 가치 문장 그대로)
</success_criteria>

<output>
Create `.planning/phases/02-golden/02-02-SUMMARY.md` when done
</output>
