---
phase: 02-golden
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/build.gradle
  - app/src/main/java/com/wf11/safealert/03_service/BleService.kt
  - app/src/test/java/com/wf11/safealert/support/BleServiceTestHarness.kt
  - app/src/test/java/com/wf11/safealert/ble/AlertCascadeGoldenTest.kt
autonomous: false
requirements: [TEST-01, TEST-02, BUG-02]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Robolectric 위에서 BleService 인스턴스가 onCreate() 없이 생성되고, processAlert 를 1프레임 호출하면 alertState 와 BROADCAST_ALERT 브로드캐스트가 관측된다 (D-2B, D-2E)"
    - "processAlert 의 시각 소스가 nowMs 기본인자로 주입 가능하며, 기본값 호출 시 런타임 동작은 변하지 않는다 (D-2C, D-2H)"
    - "골든 하네스가 processAlert 가 참조하는 DevSettings 심볼 전체를 명시 대입하므로, 출하 기본값을 바꿔도 골든 입력 구성은 흔들리지 않는다 (D-2D)"
    - "진동·소리·Firebase 저장·오버레이 부작용이 전부 차단되어 JVM 유닛 테스트가 부작용 없이 끝난다 (D-2D)"
    - statement: "processAlert 최초 프레임(칼만 워밍업 미완료)에서도 예외 없이 판정이 종료된다"
      verification: backstop
    - statement: "리플렉션·테스트 전용 코드가 app/src/main 에 단 1줄도 존재하지 않는다"
      verification: backstop
  artifacts:
    - app/src/test/java/com/wf11/safealert/support/BleServiceTestHarness.kt
    - app/src/test/java/com/wf11/safealert/ble/AlertCascadeGoldenTest.kt
    - "app/build.gradle — testImplementation org.robolectric:robolectric + testOptions.unitTests.includeAndroidResources"
    - "app/src/main/java/com/wf11/safealert/03_service/BleService.kt — processAlert nowMs 기본인자"
  key_links:
    - "Robolectric.buildService(BleService::class.java).get() → attach 완료·onCreate 미실행 서비스 인스턴스"
    - "ReflectionHelpers.callInstanceMethod(service, \"processAlert\", ...) → private 판정 함수 진입"
    - "shadowOf(RuntimeEnvironment.getApplication()).broadcastIntents → BleService.BROADCAST_ALERT 관측"
    - "DevSettings.init(RuntimeEnvironment.getApplication()) → lateinit prefs 초기화 (미호출 시 전 프로퍼티 접근 실패)"
  prohibitions:
    - "판정 반경 값(지게차 15m/8m, 그 외 5m/3m) 변경 금지"
    - "UWB 제거·비활성화 금지"
    - "iBeacon 스캔 제거 금지"
    - "Firebase 프로필 공유 제거 금지"
    - "UWB 를 主 판정으로 승격 금지"
    - "신규 기능 추가 금지"
    - "1바이트 비트팩 페이로드 레이아웃 변경 금지"
    - "기대값 자동 갱신 경로 생성 금지 (수동 재동결만)"
    - "app/src/main 에 리플렉션·테스트 전용 코드 유입 금지"
    - "processAlert 로직 이동·추출 금지 (Phase 3 REFACTOR-01 범위)"
    - "UWB 세션 6대 초과 플립 경로 골든화 금지 (BUG-03, v2 이월)"
    - "ServiceController 의 create() 호출 금지"
---

> **As a** SafeAlert 유지보수자,
> **I want to** 고정 RSSI 시퀀스를 Android 프레임워크·실기기 없이 JVM 에서 재생해 `processAlert` 의 등급·전이 시점을 기대값과 비교하고,
> **so that** 안전 크리티컬 로직을 분해하기 전에 현재 동작이 회귀 없이 보존된다는 것을 기계적으로 증명할 수 있다.
>
> 출처: REQUIREMENTS.md TEST-01 / TEST-04 원문. ROADMAP `**Goal**` 줄은 사용자 스토리 형식이 아니므로 스토리를 지어내지 않고 요구사항 원문에서 도출했다.

<objective>
Phase 2 전체가 딛고 설 최소 수직 슬라이스를 세운다. Robolectric 의존성 → `BleService` 인스턴스화 → `processAlert` 리플렉션 호출 → `alertState`/브로드캐스트 관측까지 **한 프레임**을 종단으로 관통시키고, 그 위에 DevSettings 31 심볼 명시 대입을 얹는다.

이 플랜은 02-RESEARCH.md 의 최대 리스크인 Assumption A1(`ServiceController.get()` 이 `onCreate()` 를 실행하지 않는다 — `[CITED]` 등급, 로보렉트릭 소스 미확인)을 **실제 트라이얼 테스트 1회로 실증**하는 것이 본질이다. 이 전제가 깨지면 02-02/02-03 의 설계 전체가 무효이므로 Wave 1 에서 먼저 판정한다.

Purpose: TEST-01·TEST-02·BUG-02 세 요구사항이 공통으로 요구하는 "JVM 에서 판정 로직을 재생하는 능력"을 확보한다 (TEST-04 선례 승계).
Output: Robolectric 의존성, `nowMs` 시임 2곳, `BleServiceTestHarness`, 종단 스모크 테스트 1건, 골든 DevSettings 프로파일.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-golden/02-CONTEXT.md
@.planning/phases/02-golden/02-RESEARCH.md

참조만 (대용량이므로 `<read_first>` 의 행 범위만 읽을 것):
- `app/src/main/java/com/wf11/safealert/03_service/BleService.kt` (3,899줄)
- `app/src/test/java/com/wf11/safealert/ble/RssiCascadeTest.kt` (Phase 1 골든 형식 원형 — 새 골든은 이 형식을 승계)
</context>

<constraints>
- **경로 정합 (확인 완료):** 02-CONTEXT.md `canonical_refs` 의 경로 접두사는 실제 디스크 기준으로 정정·통일됐다(02-RESEARCH.md `## Open Questions (RESOLVED)` Q3). 정답 경로는 `03_service/BleService.kt`(패키지 `com.wf11.safealert.service`), `02_ble/KalmanFilter.kt`(패키지 `com.wf11.safealert.ble`), `06_utils/UwbRanger.kt`(패키지 `com.wf11.safealert.utils`) 다. **행 번호도 전부 재확인됐다**(`processAlert` = :1406-2543) — 경로·행 번호 모두 그대로 신뢰할 것.
- **프로덕션 변경 예산:** 이 Phase 전체에서 `app/src/main` 변경은 D-2C 시그니처 1건 + D-2H 1줄 + (02-04 의) BUG-02 수정뿐이다. 그 밖의 어떤 리팩터·가시성 변경(`private` → `internal` 포함)도 금지 — `processAlert` 는 `private` 인 채로 리플렉션 호출한다.
- **`workflow.human_verify_mode = end-of-phase`:** 기능 검증용 `checkpoint:human-verify` 는 만들지 않는다. 이 플랜의 Task 1 체크포인트는 기능 게이트가 아니라 **공급망(supply-chain) 게이트**이므로 예외로 존치한다 (02-RESEARCH.md Package Legitimacy Audit 의 명시 권고).
- `BROADCAST_ALERT` 의 실제 문자열은 `"com.wf11.safealert.ALERT"` 이며 상수 `BleService.BROADCAST_ALERT`(`BleService.kt` :60-72)로만 참조한다. 02-RESEARCH.md 의 예시가 리터럴 `"BROADCAST_ALERT"` 로 잘못 적혀 있으니 따르지 말 것.
</constraints>

<artifacts_this_phase_produces>
이 플랜이 새로 만드는 심볼 (전부 `app/src/test/` 범위, 예외 1건은 프로덕션 파라미터):

| 심볼 | 위치 | 종류 |
|------|------|------|
| `nowMs: () -> Long` | `03_service/BleService.kt` `processAlert` 시그니처 | 프로덕션 기본인자 (유일한 프로덕션 신규 심볼) |
| `object BleServiceTestHarness` | `test/.../support/BleServiceTestHarness.kt` | 테스트 헬퍼 오브젝트 |
| `BleServiceTestHarness.newService()` | 동상 | `BleService` 인스턴스 생성 (onCreate 미실행) |
| `BleServiceTestHarness.callProcessAlert(...)` | 동상 | private `processAlert` 리플렉션 진입점 |
| `BleServiceTestHarness.alertLevelOf(service, deviceId)` | 동상 | `alertState` 레벨 관측 |
| `BleServiceTestHarness.alertEntryMsOf(service, deviceId)` | 동상 | `alertState` 진입시각 관측 (부재 시 null) |
| `BleServiceTestHarness.alertBroadcasts()` | 동상 | `BROADCAST_ALERT` 브로드캐스트 목록 |
| `BleServiceTestHarness.applyGoldenDevSettings()` | 동상 | DevSettings 골든 프로파일 |
| `BleServiceTestHarness.resetBetweenTests(service)` | 동상 | 브로드캐스트 목록·상태맵 초기화 |
| `class AlertCascadeGoldenTest` | `test/.../ble/AlertCascadeGoldenTest.kt` | 골든 테스트 클래스 (스모크 + 부작용 무해화) |
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Robolectric 공급망 검증 (차단형 체크포인트)</name>
  <read_first>
    - `.planning/phases/02-golden/02-RESEARCH.md` — `## Package Legitimacy Audit` 절 전체
  </read_first>
  <what-built>
    아직 아무것도 만들지 않았다. 이 체크포인트는 Phase 02 가 저장소에 처음으로 외부 의존성(`org.robolectric:robolectric`)을 들이기 직전의 차단 지점이다. 이 시점의 저장소 상태는 Phase 01 종료 시점 그대로이며, `app/build.gradle` 은 아직 손대지 않았다.
  </what-built>
  <how-to-verify>
    1. 브라우저에서 `https://mvnrepository.com/artifact/org.robolectric/robolectric` 또는 Maven Central 검색으로 좌표 `org.robolectric:robolectric` 과 버전 `4.15.1` 이 실재하는지 확인한다.
    2. 그 좌표의 공식 소스 저장소가 `github.com/robolectric/robolectric` 인지 확인한다 (배포자 계정과 저장소가 일치하는지).
    3. 이 좌표가 `testImplementation` 스코프로만 추가되어 릴리스 APK 에 포함되지 않는다는 점에 동의하는지 판단한다. `minifyEnabled false` 라 프로덕션에 들어간 것은 무엇이든 그대로 노출되므로, 스코프 한정이 이 위험의 유일한 방어선이다.
  </how-to-verify>
  <resume-signal>
    "승인" 또는 "approve" 를 입력하면 기본 버전 `4.15.1` 로 Task 2 를 진행한다. 다른 버전을 쓰려면 "승인 &lt;버전&gt;" 형태로 지정한다. "거부" 또는 다른 패키지 지시가 오면 Task 2 를 진행하지 않고 정지한다.
  </resume-signal>
  <action>
    실행자는 여기서 멈추고 사람에게 승인을 요청한다. 02-RESEARCH.md 의 Package Legitimacy Audit 은 `org.robolectric:robolectric` 을 "OK / Approved" 로 판정했으나 Source Repo 칸이 `[ASSUMED: 훈련 지식 — 저장소 자체 존재는 미재확인]` 이다. `[ASSUMED]` 등급 패키지는 설치 전 사람 확인이 필수이며 auto_advance 로 자동 승인되지 않는다.

    사람에게 제시할 확인 항목: (1) Maven Central 좌표 `org.robolectric:robolectric` 버전 `4.15.1` 이 실재하는가, (2) 공식 소스 저장소가 github.com/robolectric/robolectric 인가, (3) 이 좌표를 `testImplementation` 스코프로만 추가한다는 점(프로덕션 APK 미포함, `minifyEnabled false` 와 무관)에 동의하는가.

    사람이 승인하지 않으면 Task 2 로 진행하지 않는다. 사람이 다른 버전을 지시하면 그 버전을 채택하고 SUMMARY 에 기록한다.
  </action>
  <verify>
    <human-check>사람이 위 3개 항목을 확인하고 "승인" 을 명시 입력했는가. 승인 문구와 확정 버전을 SUMMARY 에 그대로 기록한다.</human-check>
  </verify>
  <acceptance_criteria>
    - 사람의 명시 승인 문구가 SUMMARY 에 인용되어 있다
    - 확정 버전 문자열이 SUMMARY 에 기록되어 있다 (기본 `4.15.1`)
    - 승인 없이 `app/build.gradle` 이 수정되지 않았다
  </acceptance_criteria>
  <done>Robolectric 좌표·버전이 사람 승인으로 확정되어 Task 2 의 의존성 추가가 허가된 상태.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: 종단 슬라이스 — Robolectric 의존성 + nowMs 시임 + 하네스 + 1프레임 스모크</name>
  <precondition>`java -version` 이 JDK 17 을 보고하고, Maven Central 및 Robolectric `android-all` 아티팩트에 대한 네트워크 접근이 가능하다 (최초 1회 빌드는 오프라인 불가 — 02-RESEARCH.md Environment Availability 의 `[ASSUMED]` 항목). 둘 중 하나라도 불성립이면 즉시 중단하고 사용자에게 보고한다.</precondition>
  <read_first>
    - `app/build.gradle` :70-90 (테스트 의존성 블록, `android { }` 블록 위치 확인)
    - `app/src/main/java/com/wf11/safealert/03_service/BleService.kt` :55-80 (`BROADCAST_ALERT`, `EXTRA_ID`, `EXTRA_ALERT_LEVEL`, `EXTRA_DISPLAY_NAME` 상수)
    - `app/src/main/java/com/wf11/safealert/03_service/BleService.kt` :380-390 (`alertState` 선언)
    - `app/src/main/java/com/wf11/safealert/03_service/BleService.kt` :1100-1125 (`processAlert` 호출부)
    - `app/src/main/java/com/wf11/safealert/03_service/BleService.kt` :1406-1425 (시그니처 + `now` 지역변수)
    - `app/src/main/java/com/wf11/safealert/03_service/BleService.kt` :1505-1520 (`kfVel` / `lastApproachAtMs`)
    - `app/src/main/java/com/wf11/safealert/06_utils/DevSettings.kt` :60-80 (`lateinit prefs`, `init(context)`)
    - `app/src/main/java/com/wf11/safealert/02_ble/KalmanFilter.kt` :1-30 (Phase 1 `nowMs` 기본인자 선례 — 이 형태를 그대로 승계)
    - `app/src/test/java/com/wf11/safealert/ble/RssiCascadeTest.kt` :6-17 (골든 헤더 주석 규약) 및 :130-173 (`runCascade`/`assertCascade` 셰어드 헬퍼 원형 — 이 하네스가 잇는 Phase 1 패턴)
  </read_first>
  <files>app/build.gradle, app/src/main/java/com/wf11/safealert/03_service/BleService.kt, app/src/test/java/com/wf11/safealert/support/BleServiceTestHarness.kt, app/src/test/java/com/wf11/safealert/ble/AlertCascadeGoldenTest.kt</files>
  <behavior>
    - 스모크 1 (Assumption A1 실증): `Robolectric.buildService(BleService::class.java).get()` 로 얻은 인스턴스의 `lifecycle.currentState` 가 `Lifecycle.State.INITIALIZED` 다. (`CREATED` 이면 `onCreate()` 가 실행된 것 → Assumption A1 붕괴)
    - 스모크 1-b (`onCreate()` 부작용 부재 3종): 위 인스턴스를 얻은 직후 `onCreate()` 가 남겼어야 할 흔적이 셋 다 없다 — (i) `shadowOf(RuntimeEnvironment.getApplication()).registeredReceivers` 에 이 인스턴스가 등록한 리시버가 0건이다 (`onCreate()` 는 4건 등록), (ii) 알림 채널이 생성되지 않았다, (iii) `shadowOf(RuntimeEnvironment.getApplication()).broadcastIntents` 가 비어 있다.
    - 스모크 2: 강한 RSSI 를 `processAlert` 에 2프레임 연속 투입하면 2콜째에 `alertState[deviceId]` 가 non-null 이고, `Pair` 의 첫 원소가 유효 등급 상수다. [정정 2026-08-28 — 초안의 "1프레임"은 현 프로덕션 코드에서 성립 불가: `dangerStreak`(BleService.kt:1627)은 첫 프레임에 1, `fastContact`(:2326/:2329)는 >=2 를 요구하고 동시에 `warmingUp`(:1524)=true 이므로 `shouldAlert`(:2334) 의 두 분기가 모두 불만족. 1콜=미등록, 2콜=등록 확증이 유일하게 가능한 구현]
    - 스모크 3: 같은 2콜째 호출로 `BROADCAST_ALERT` 브로드캐스트가 1건 이상 관측되고 `EXTRA_ID` 가 넣은 deviceId 와 일치한다.
    - 스모크 4: `nowMs` 에 고정 시각 람다를 주입하면 `alertState` 진입시각이 그 고정 시각과 같다. (시임이 실제로 물렸다는 증거)
    - 스모크 5: 진동·소리·Firebase·오버레이 관련 예외 없이 위 4건이 통과한다.
  </behavior>
  <action>
    **(a) `app/build.gradle`** — 기존 `testImplementation 'junit:junit:4.13.2'` 줄 바로 아래에 Task 1 에서 확정된 Robolectric 좌표를 `testImplementation` 스코프로 추가한다 (D-2A). 같은 파일 `android { }` 블록 안에 `testOptions { unitTests { includeAndroidResources = true } }` 를 추가한다 — Robolectric 이 병합 매니페스트와 리소스를 읽는 데 필요하다. `androidx.test.ext:junit` 은 **추가하지 않는다**: 02-RESEARCH.md Pitfall 4 대로 `@RunWith(RobolectricTestRunner::class)` 만 쓰므로 불필요하며, 기존 좌표는 `androidTestImplementation` 스코프에 있다. 프로덕션(`implementation`) 의존성은 한 줄도 건드리지 않는다.

    빌드 실패 시 허용된 유일한 조정: Task 1 에서 확정된 **같은 좌표의 다른 버전**으로 하향(AGP 8.3.2 / Gradle 8.6 호환 문제 시). 다른 라이브러리로의 대체는 금지 — Task 1 승인 범위를 벗어난다. 조정했다면 SUMMARY 에 사유와 최종 버전을 기록한다.

    **(a-2) Assumption A1 선판정 트라이얼 — (b) 착수 전 게이트** — 의존성이 붙자마자, 다른 코드를 한 줄도 더 쓰기 전에 트라이얼 테스트 **1건만** 먼저 만들어 돌린다. 02-RESEARCH.md Open Question 1 이 `.create()` 미호출 시 `onCreate()` 스킵을 `[CITED]` 등급으로만 남겨두고 "계획 단계 첫 태스크로 트라이얼 테스트 1개"를 권고했기 때문이며, 02-02 · 02-03 · 02-04 가 전부 이 가정 위에 서 있다. 절차: (1) `@RunWith(RobolectricTestRunner::class)` 클래스에 `assumptionA1_getWithoutCreate_staysInitialized()` 하나만 두고 `Robolectric.buildService(BleService::class.java).get()` 로 인스턴스를 얻는다 — `create()` 는 부르지 않는다. (2) `lifecycle.currentState` 를 읽어 `Lifecycle.State.INITIALIZED` 임을 단언하고, 이어서 `<behavior>` 스모크 1-b 의 부작용 부재 3종을 단언한다. (3) 관측된 `currentState` 값과 3종 단언 결과를 SUMMARY 의 전용 절 `## Assumption A1 판정` 에 그대로 적는다 — `[CITED]` 가정을 이 Phase 에서 실측으로 승격시킨 근거이자 후속 3개 플랜의 전제 증거다. **이 트라이얼이 통과해야만 (b)~(d) 로 진행한다.** 실패하면 (b) 로 가지 말고 곧장 (e) 로 간다.

    **(b) `03_service/BleService.kt` 시임 2곳 (D-2C, D-2H)** — `processAlert` 시그니처(:1406)의 **마지막 파라미터**로 `nowMs: () -> Long = { System.currentTimeMillis() }` 를 추가한다. `KalmanFilter.kt` 의 동일 패턴을 형태 그대로 승계한다. :1422 의 `now` 지역변수 초기화식을 `nowMs()` 호출로 바꾸고, :1512 의 `lastApproachAtMs` 대입식도 `nowMs()` 로 바꾼다. 호출부(:1113-1117)는 **수정하지 않는다** — 기본 인자가 있으므로 컴파일·런타임 동작 변화가 0 이다. `processAlert` 본문(:1406-2543) 밖의 시각 호출, `freshUwbDistM`(:2566-2570), `uwbJudgeModeExclusive`(:2554-2560) 의 시각 소스는 **건드리지 않는다** (D-4C 가 별도 경로로 처리). `private` 가시성은 유지한다.

    **(c) `BleServiceTestHarness.kt`** — 패키지 `com.wf11.safealert.support`, `object BleServiceTestHarness`. 구성:
    - `newService(): BleService` — `Robolectric.buildService(BleService::class.java).get()` 로 인스턴스를 얻는다. `create()` 나 그 밖의 라이프사이클 전진 메서드는 호출하지 않는다. 반환 전에 `DevSettings.init(RuntimeEnvironment.getApplication())` 를 1회 호출한다 (`DevSettings.prefs` 가 `lateinit` 이므로 미호출 시 첫 프로퍼티 접근에서 실패).
    - `callProcessAlert(service, deviceId, rssi, remoteState, remoteTurn, payloadPresent, peerEchoRssi, nowMs)` — `ReflectionHelpers.callInstanceMethod` 로 `"processAlert"` 를 호출한다. Kotlin 기본인자는 합성 `$default` 브리지에 있으므로 **모든 파라미터를 명시**해야 한다. `ClassParameter.from` 의 JVM 타입은 각각 `String`, `int`, `int`, `int`, `boolean`, `int`, 그리고 `nowMs` 는 `kotlin.jvm.functions.Function0`. 호출자는 `remoteState`/`remoteTurn`/`payloadPresent`/`peerEchoRssi` 를 프로덕션 기본값과 같은 값으로 넘기며, 그 기본값은 시그니처(:1406)에서 읽어 하네스 상수로 고정한다.
    - `alertLevelOf(service, deviceId): Int?` / `alertEntryMsOf(service, deviceId): Long?` — `ReflectionHelpers.getField(service, "alertState")` 로 얻은 맵에서 `Pair` 의 first/second 를 꺼내고, 엔트리 부재 시 `null` 을 반환한다 (D-2G).
    - `alertBroadcasts(): List<Intent>` — `shadowOf(RuntimeEnvironment.getApplication()).broadcastIntents` 에서 `action == BleService.BROADCAST_ALERT` 인 것만 필터링해 발생 순서대로 반환한다.
    - `resetBetweenTests(service)` — `shadowOf(...).clearBroadcastIntents()` 와 `alertState` 클리어. 테스트 간 상태 누수를 막는다.

    이 하네스는 Phase 1 의 셰어드 헬퍼 패턴을 잇는 것이다 — `RssiCascadeTest.kt` 의 private `runCascade`(:130-156) / `assertCascade`(:158-173) 가 한 테스트 파일 안에 갇혀 있던 것을, 02-02 · 02-03 · 02-04 가 함께 쓸 수 있도록 `object` 로 끌어올린 형태다. 02-02 가 이 위에 `runScenario`/`assertScenario` 를 얹으므로(02-PATTERNS.md), 이 Task 의 하네스는 그 두 헬퍼가 올라탈 관측면(`alertLevelOf` / `alertEntryMsOf` / `alertBroadcasts`)과 초기화(`newService` / `resetBetweenTests`)까지만 제공하고 시나리오 루프 자체는 만들지 않는다.

    파일 상단에 Phase 1 `RssiCascadeTest.kt` :6-17 헤더 규약과 같은 형식의 주석 블록을 둔다: 기록 방식은 Phase 1 D-09 의 record-then-freeze(1회 기록·동결)를 그대로 확장한 것이라는 명시, 재동결은 사람이 이 파일을 직접 편집하는 것뿐이고 자동 갱신 경로는 만들지 않는다는 명시, 관측면 정의(D-2E).

    **(d) `AlertCascadeGoldenTest.kt`** — 패키지 `com.wf11.safealert.ble`, `@RunWith(RobolectricTestRunner::class)`. `<behavior>` 의 스모크 1~5 를 테스트 메서드로 구현한다. 이 시점의 기대값은 등급 시계열 골든이 아니라 **배관이 살아 있다는 최소 단언**이다 — 전체 시계열 골든은 02-02 가 담당한다.

    **(e) Assumption A1 붕괴 시 대응** — 스모크 1 이 `INITIALIZED` 가 아니거나 `sendBroadcast` 에서 base context 미부착 예외가 나면, 먼저 `ReflectionHelpers.callInstanceMethod(service, "attachBaseContext", ClassParameter.from(Context::class.java, RuntimeEnvironment.getApplication()))` 를 시도한다. 그래도 `onCreate()` 실행이 불가피하다면 **중단하고 사용자에게 보고한다** — D-2B 를 우회해 `create()` 를 부르는 선택은 실행자 재량이 아니다 (registerReceiver ×4 + 알림 채널 + Firebase `loadEchoPriors` + `ImuFusion.init` 이 함께 돌아가 골든의 결정성이 깨진다).
  </action>
  <reversibility rating="reversible">기본인자 시임 2곳은 호출부 무영향이고 프로덕션 동작 변화가 0 이다. `git revert` 1커밋으로 완전 원복되며, Phase 1 의 `KalmanFilter` 시임이 같은 방식으로 이미 검증됐다.</reversibility>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*.AlertCascadeGoldenTest.assumptionA1_*"</automated>
    <automated>./gradlew testDebugUnitTest --tests "*.AlertCascadeGoldenTest"</automated>
    <automated>./gradlew testDebugUnitTest</automated>
    <automated>test 0 -eq $(grep -rn "ReflectionHelpers\|Robolectric\|robolectric" app/src/main | wc -l)</automated>
  </verify>
  <acceptance_criteria>
    - Assumption A1 판정 결과(관측된 `lifecycle.currentState` 값 + `onCreate()` 부작용 부재 3종의 단언 결과)가 SUMMARY 의 `## Assumption A1 판정` 절에 기록됐다
    - `AlertCascadeGoldenTest` 의 스모크 1 · 1-b · 2~5 가 모두 통과한다
    - 기존 17건 골든(`RssiCascadeTest` 8 / `RssiCascadeIsolationTest` 3 / `MedianFilterWarmupTest` 6)이 전부 그대로 통과한다 — 시임이 동작을 바꾸지 않았다는 증거
    - `app/src/main` 에 Robolectric·ReflectionHelpers 참조가 0건이다 (T-02-01 완화)
    - `git diff --stat` 상 `app/src/main` 변경은 `BleService.kt` 1파일뿐이고, 변경 줄 수가 시그니처 1건 + 본문 2줄 규모다
    - `app/build.gradle` 의 신규 좌표가 `testImplementation` 스코프에만 있다
  </acceptance_criteria>
  <done>`BleService` 를 JVM 에서 부작용 없이 인스턴스화해 `processAlert` 한 프레임을 통과시키고, 그 결과를 `alertState` 와 `BROADCAST_ALERT` 두 관측면에서 읽어내며, 주입한 시계가 실제로 반영되는 것까지 종단으로 증명된 상태. 02-RESEARCH.md Assumption A1 / Open Question 1 이 실증으로 해소되었다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 골든 DevSettings 프로파일 — 판정 참조 심볼 전체 명시 대입 + 부작용 무해화 단언</name>
  <read_first>
    - `app/src/main/java/com/wf11/safealert/06_utils/DevSettings.kt` :100-130 (`beaconGainPercent`, `val beaconGainDbm` 파생식, 기본값 상수)
    - `.planning/phases/02-golden/02-CONTEXT.md` — D-2D 항목 전문
    - `app/src/main/java/com/wf11/safealert/06_utils/OverlayManager.kt` :135-150 (`showSidebar` 의 `canDrawOverlays` 조기 return)
  </read_first>
  <files>app/src/test/java/com/wf11/safealert/support/BleServiceTestHarness.kt, app/src/test/java/com/wf11/safealert/ble/AlertCascadeGoldenTest.kt</files>
  <behavior>
    - 부작용 1: 골든 프로파일 적용 후 `DevSettings.vibrationEnabled` 와 `DevSettings.soundEnabled` 가 false 다.
    - 부작용 2: `DevSettings.autoSaveAlerts` 가 false 다 (Firebase `saveAlert` 2회 차단).
    - 부작용 3: `Settings.canDrawOverlays(context)` 가 false 다 (오버레이 9회 조기 return).
    - 완전성: `processAlert` 본문이 참조하는 `DevSettings` 심볼 중 대입 가능한 것 전부가 `applyGoldenDevSettings()` 안에서 명시 대입된다.
    - 결정성: `applyGoldenDevSettings()` 를 두 번 호출해도 같은 구성이 나오고, 두 번째 호출 후 스모크 테스트 결과가 첫 번째와 동일하다.
  </behavior>
  <action>
    `BleServiceTestHarness.applyGoldenDevSettings()` 를 구현한다. 대입 대상 심볼 집합은 추측하지 않고 **grep 으로 도출**한다 — 권위 명령:
    `sed -n '1406,2543p' app/src/main/java/com/wf11/safealert/03_service/BleService.kt | grep -o "DevSettings\.[A-Za-z_][A-Za-z0-9_]*" | sort -u`

    현재 이 명령의 결과는 31개이며 D-2D 의 "31개" 와 정확히 일치한다. 이 31개 중 특수 처리 3건:
    - `DevSettings.KALMAN_PRESET_FAST` — 상수 참조이지 설정값이 아니다. 대입 대상 아님.
    - `DevSettings.beaconGainDbm` — 유일한 `val`(파생식 `(beaconGainPercent - 100) / 5`). `beaconGainPercent` 를 대입해 간접 고정한다 (D-2D).
    - `DevSettings.kalmanPreset` — 프리셋 상수 중 하나를 명시 선택한다. 어떤 프리셋을 고르는지가 BUG-02 재현 가능성을 좌우하므로, 출하 기본 프리셋과 동일한 값을 고르고 그 근거를 주석 1줄로 남긴다.

    따라서 실제 대입은 `var` 29개 + `beaconGainPercent` 1개다. 각 대입값은 **현행 출하 기본값과 같은 값**으로 시작하되, 부작용 3종(`vibrationEnabled`, `soundEnabled`, `autoSaveAlerts`)만 false 로 못 박는다. `rssiWarning` 같은 판정 임계는 기본값을 그대로 명시해야 골든이 "출하 구성의 동작" 을 고정한다.

    grep 결과가 31개가 아니면(코드가 이동한 경우) **grep 결과를 권위로 채택**하고 D-2D 의 숫자를 근사치로 취급하되, 차이를 SUMMARY 에 기록한다.

    각 대입 줄은 한 줄에 하나씩, 알파벳 순으로 둔다 — 나중에 위 grep 결과와 눈으로 대조 가능해야 한다. 값 뭉치를 상수 객체로 추출하거나 리플렉션으로 일괄 세팅하지 않는다(소비자 1곳에 대한 추상화 금지, 그리고 명시성이 D-2D 의 목적 그 자체다).

    `AlertCascadeGoldenTest` 에 `<behavior>` 의 부작용 1~3 과 결정성 단언을 테스트로 추가한다. 완전성 단언은 테스트 코드가 아니라 `<verify>` 의 자동 검증으로 처리한다.

    `newService()` 가 `applyGoldenDevSettings()` 를 호출하도록 배선해, 이후 모든 골든 테스트가 자동으로 같은 구성 위에서 돌게 한다.
  </action>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "*.AlertCascadeGoldenTest"</automated>
    <automated>test $(sed -n '1406,2543p' app/src/main/java/com/wf11/safealert/03_service/BleService.kt | grep -o "DevSettings\.[A-Za-z_][A-Za-z0-9_]*" | sort -u | grep -v "KALMAN_PRESET_FAST" | grep -v "beaconGainDbm" | wc -l) -le $(grep -v '^\s*//' app/src/test/java/com/wf11/safealert/support/BleServiceTestHarness.kt | grep -o "DevSettings\.[A-Za-z_][A-Za-z0-9_]* *=" | sort -u | wc -l)</automated>
  </verify>
  <acceptance_criteria>
    - `processAlert` 가 참조하는 대입 가능한 `DevSettings` 심볼 전부가 `applyGoldenDevSettings()` 안에서 명시 대입되어 있다 (위 두 grep 카운트 비교로 증명)
    - `beaconGainDbm` 은 `beaconGainPercent` 를 통해 간접 고정되어 있다
    - 진동·소리·Firebase 저장·오버레이 부작용 4종이 전부 차단됨이 테스트로 단언되어 있다
    - `newService()` 를 부르면 골든 프로파일이 자동 적용된다
    - 기존 17건 골든이 여전히 통과한다
  </acceptance_criteria>
  <done>골든 테스트가 자족적이다 — 하네스 파일만 읽으면 어떤 DevSettings 구성 위에서 동작을 고정했는지 전부 알 수 있고, 출하 기본값이 바뀌어도 골든 입력이 흔들리지 않는다. 02-02/02-03 이 곧바로 시계열 기록에 들어갈 수 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 외부 아티팩트 저장소 → 빌드 툴체인 | 신규 테스트 의존성(Robolectric + 그 전이 의존성, `android-all` jar)이 빌드에 유입된다 |
| 테스트 코드 → 프로덕션 코드 | 리플렉션·테스트 전용 코드가 `app/src/main` 으로 새어 나갈 수 있는 경계 |
| CI 실행 → 공개 아티팩트 | 테스트 리포트 XML/HTML 이 GitHub Actions 아티팩트로 남는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | `org.robolectric:robolectric` (Maven Central) | high | mitigate | Task 1 의 `gate="blocking-human"` 체크포인트에서 좌표·버전·공식 저장소를 사람이 확인한 뒤에만 `app/build.gradle` 수정. `[ASSUMED]` 등급이므로 auto_advance 무시. 대체 라이브러리로의 이탈 금지 |
| T-02-01 | Tampering | `app/src/main` 리플렉션 유입 | medium | mitigate | Task 2 `<verify>` 의 `grep -rn "ReflectionHelpers\|Robolectric" app/src/main` 이 0건임을 게이트로 강제. `minifyEnabled false` 라 필드명이 릴리스 APK 에 노출되므로 테스트 전용 접근 코드가 프로덕션에 남으면 공격면이 된다 |
| T-02-02 | Information Disclosure | CI 테스트 리포트 아티팩트 | low | mitigate | 입력 시퀀스는 하드코딩된 합성 상수만 사용. 현장 로그·실측 RSSI 덤프·기기 식별자 원본을 테스트 픽스처에 넣지 않는다. deviceId 는 `"AA:BB:CC:DD:EE:01"` 류 합성 MAC 만 사용 |
| T-02-03 | Tampering | `processAlert` 시그니처 변경으로 인한 프로덕션 동작 변화 | medium | mitigate | 기본인자 방식으로 호출부 무수정. 기존 17건 골든 전수 통과를 수용 기준에 포함해 동작 불변을 기계적으로 증명 |
| T-02-04 | Denial of Service | 최초 빌드의 네트워크 의존(`android-all` jar 다운로드) | low | accept | 개발/CI 환경 모두 온라인 전제. Task 2 `<precondition>` 으로 명시하고 불성립 시 즉시 중단 |
</threat_model>

<verification>
1. `./gradlew testDebugUnitTest` 전체가 통과한다 (기존 17건 + 신규 스모크).
2. `app/src/main` 변경이 `03_service/BleService.kt` 1파일, 시그니처 1건 + 본문 2줄로 한정된다.
3. `grep -rn "ReflectionHelpers\|Robolectric" app/src/main` 이 0건이다.
4. `app/build.gradle` 의 신규 의존성이 `testImplementation` 스코프에만 존재한다.
5. Task 1 체크포인트의 사람 승인 문구가 SUMMARY 에 인용되어 있다.
</verification>

<success_criteria>
- Robolectric 위에서 `BleService.processAlert` 한 프레임이 종단으로 실행되고 두 관측면에서 읽힌다
- `nowMs` 주입이 실제로 판정 시각을 지배한다 (진입시각 단언으로 증명)
- DevSettings 골든 프로파일이 완전하고 자족적이다
- 기존 17건 골든이 한 건도 흔들리지 않는다
- 02-RESEARCH.md Assumption A1 이 `[CITED]` 에서 실증으로 승격되었다 (또는 붕괴가 확인되어 사용자에게 보고되었다)
</success_criteria>

<output>
Create `.planning/phases/02-golden/02-01-SUMMARY.md` when done
</output>
