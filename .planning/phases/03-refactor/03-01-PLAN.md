---
phase: 03-refactor
plan: 01
type: execute
wave: 1
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - app/src/main/java/com/wf11/safealert/03_service/BleService.kt
  - app/src/main/java/com/wf11/safealert/03_service/CalibrationEngine.kt
  - app/src/main/java/com/wf11/safealert/03_service/UwbDistanceManager.kt
  - app/src/main/java/com/wf11/safealert/03_service/AlertStateMachine.kt
  - app/src/main/java/com/wf11/safealert/05_ui/BleSettingsActivity.kt
  - app/src/test/java/com/wf11/safealert/ble/AlertStateMachineJvmTest.kt
autonomous: false
requirements: [REFACTOR-01, REFACTOR-02, REFACTOR-03, REFACTOR-04]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "에코 RSSI 보정 상태·영속화·업로드가 BleService 밖의 단일 소유자(CalibrationEngine)로 옮겨지고, BleSettingsActivity 의 5개 참조가 새 소유자를 직접 가리킨다 (REFACTOR-03)"
    - "UWB 표본 신선도 판정(freshUwbDistM)과 배타판정 게이트(uwbJudgeModeExclusive)가 UwbDistanceManager 로 옮겨지고, UwbRanger 는 단 1줄도 수정되지 않는다 (REFACTOR-02)"
    - "AlertStateMachine 이 Android Context·Service 없이 생성되고, 순수 JVM 테스트가 Robolectric 없이 경보 등급 판정을 1프레임 이상 구동한다 (REFACTOR-01)"
    - "기존 골든·회귀 테스트 42건이 테스트 파일 1줄도 수정되지 않은 채 전부 통과한다 — 기대값 배열 불변 (REFACTOR-04)"
    - statement: "processAlert 진입점 이름·파라미터가 보존되어 BleServiceTestHarness 가 무수정 동작한다"
      verification: backstop
    - statement: "스캔 콜백 스레딩 모델이 변하지 않는다 — 새 스레드·큐·디스패처 도입 0건"
      verification: backstop
  artifacts:
    - app/src/main/java/com/wf11/safealert/03_service/CalibrationEngine.kt
    - app/src/main/java/com/wf11/safealert/03_service/UwbDistanceManager.kt
    - app/src/main/java/com/wf11/safealert/03_service/AlertStateMachine.kt
    - app/src/test/java/com/wf11/safealert/ble/AlertStateMachineJvmTest.kt
  key_links:
    - "BleService.processAlert(...) → AlertStateMachine.processAlert(...) 1줄 위임 (리플렉션 진입점 보존)"
    - "AlertStateMachine.Effects → 진동·브로드캐스트·오버레이·볼륨·표시명 부작용 경계"
    - "UwbDistanceManager(rangerSupplier) → UwbRanger.uwbDistances 조회 단일 창구"
    - "object CalibrationEngine.init(context) ← DevSettings·BeaconRegistry·UwbCalibrator 초기화 관례 승계"
  prohibitions:
    - "판정 반경 값(지게차 15m/8m, 그 외 5m/3m) 변경 금지"
    - "판정 로직 수정 금지 — 이동만 허용 (조건식·상수·평가 순서 1자 변경 금지)"
    - "기대값 배열 수정 금지 (REFACTOR-04 수용 게이트)"
    - "기존 테스트 6파일 수정 금지 (불가피 시 사용자 승인 필수)"
    - "스레딩 변경 금지 (PERF-01 은 Phase 5)"
    - "UwbRanger.kt 수정 금지 (Q2 결정: 잔여 조각만 이관)"
    - "UwbCalibrator.kt 수정 금지 (Q2 결정: 잔여 조각만 이관)"
    - "신규 기능 추가 금지"
    - "1바이트 비트팩 페이로드 레이아웃 변경 금지"
    - "브랜치 생성·전환 금지, --no-verify 금지, 커밋은 사용자 요청 시에만"
    - "app/src/main 에 리플렉션·테스트 전용 코드 유입 금지"
    - "미커밋 잔여물(.gitignore, 01-ci/01-UAT.md, 01-ci/01-VALIDATION.md) 수정 금지"
---

> **As a** SafeAlert 유지보수자,
> **I want to** BleService.kt 3,934줄 중 경보 판정·UWB 신선도·에코 보정을 각각 단독 소유자 클래스로 분리하고,
> **so that** 판정 로직을 Android 런타임 없이 읽고 테스트하고 고칠 수 있으며, 한 증상을 고칠 때 무관한 상태 필드 수십 개를 동시에 헤아리지 않아도 된다.
>
> 출처: ROADMAP.md:88-105 Phase 3 (REFACTOR-01~04).

<objective>
Phase 2 에서 세운 골든 42건을 불변 계약으로 삼아, BleService 에서 세 덩어리를 순수 이동시킨다. 로직 수정은 0 — 이동 전후 테스트 출력이 한 글자도 달라지지 않는 것이 성공 정의다.

순서는 피호출자 → 호출자다. processAlert 가 freshUwbDistM·uwbJudgeModeExclusive·echoCalAppliedDb·recordEchoDiff 를 소비하므로, 작은 협력자를 먼저 떼어내면 큰 덩어리를 옮길 때 그것들이 이미 주입된 의존이 되어 BleService 로 되돌아오는 콜백이 생기지 않는다.

Output: 신규 3파일, 기존 6테스트 무수정 통과, Robolectric 비의존 테스트 1건 신설.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
기준선: master, HEAD=c14d2cc, `git status --porcelain -- app/` 공란, 테스트 42건 통과.

정찰 확정 좌표 (Grep, 2026-08-28):

| 대상 | 좌표 | 비고 |
|---|---|---|
| processAlert | :1418~2586 (1,169줄) | 리플렉션 진입점 — 이름·시그니처 보존 필수 |
| 판정 헬퍼 | calcLevelWithHysteresis:1240, computePayloadRiskOffset:1266, estimateTTC:1327, updateTrackingState:1347, applyDepartingHysteresis:1398 | processAlert 전용 |
| uwbJudgeModeExclusive | :2587 | 호출처 1627, 2611 |
| freshUwbDistM | :2599 | 호출처 1101, 1603, 1942, 1992, 2032, 3156, 3392, 3409 |
| onUwbSampleReceived | :2608 | UwbRanger 콜백 배선 :3610 |
| judgeUwbOnly | :2628~2853 | 호출처 :2614 단 1곳 |
| 에코 companion | echoDiffLive:108, parseEchoBlob:112, serializeEchoBlob:128, echoQuantileDb:155, echoCalLocalDb:172, echoCalPriorDb:184, echoCalAppliedDb:194, class EchoDiffStats:202 | |
| 에코 인스턴스 함수 | pushRssiEcho:732, echoPrefs:753, persistEchoAll:756, persistEchoEntry:765, recordEchoDiff:772, maybeUploadEchoCalib:801, loadEchoPriors:821 | |
| UWB 필드 | peerUwbSeenMap:690, uwbSampleAtMsMap:691, uwbSafeStreakMap:692 | |
| uwbPairKeyFor | :268 | 역할쌍 오프셋 조회 |

외부 참조 (분해 시 함께 갱신):
- BleSettingsActivity.kt :204, :383, :385, :398, :401 → BleService.echoDiffLive / parseEchoBlob / echoCalLocalDb / echoCalPriorDb

부작용 표면 (processAlert 1418~2586 내부, Grep 실측):
- VibrationHelper.vibrateDanger(this) :1787 :2326 :2392 :2538
- VibrationHelper.vibrateRapidApproach(this) :2327
- VibrationHelper.vibrateWarning(this) :2418 :2559
- VibrationHelper.stopVibration(this) :2128 :2223 :2265
- sendAlertBroadcast(...) :1791 :2124 :2262 :2332 :2550 :2572
- SystemClock.elapsedRealtime() :1441 :1464 :1743 :2092 :2240
- Intent·Handler·getSystemService·SharedPreferences 직접 호출 0건 — 포트 경계가 얇다는 근거

메서드 의존 표면 (호출 횟수 · 선언 줄):
updateFloatingOverlay 9:3164 / extractDisplayName 7:3254 / sendAlertBroadcast 6:3644 / isDwellMuted 5:3044 / sendStatusBroadcast 4:3240 / isDeviceMuted 4:3004 / forceAlarmVolume 4:2922 / resyncSoundToRemaining 3:288 / collapseOverlay 3:3173 / clearDwellMute 3:3048 / updateDwellMute 2:3019 / makeStateLabel 2:3342 / getCurrentMaxLevel 1:247 / getAudibleMaxLevel 1:255 / broadcastDeviceList 1:3369 / recentPeakRssi 1:528 / oneSecAvgRssi 1:516 / recordEchoDiff 1:772
</context>

<constraints>
- 이동만, 수정 없음. 조건식·상수·평가 순서·로그 문구를 바꾸지 않는다. 컴파일을 위한 수신자 치환(this.foo() → fx.foo())만 허용.
- 진입점 보존. BleService.processAlert 는 같은 이름·같은 파라미터로 남고 본문만 1줄 위임이 된다. 골든 하네스가 리플렉션으로 이 이름을 잡으므로 하네스 무수정이 목표다.
- 한 단위 = 한 게이트. 각 태스크 종료 시 `./gradlew testDebugUnitTest` 42건 전부 통과. 실패 1건이라도 다음 태스크로 넘어가지 않는다.
- 커밋 금지. 사용자가 명시 요청할 때만 커밋한다.
- 줄 번호는 편집 직전 `grep -n` 으로 재확인한다 (이동이 누적되며 위 좌표는 즉시 낡는다).
- Read 는 offset·limit 200줄 이내.
</constraints>

<artifacts_this_phase_produces>

| 산출물 | 경로 | 소비자 |
|---|---|---|
| CalibrationEngine | 03_service/CalibrationEngine.kt | BleService, BleSettingsActivity |
| UwbDistanceManager | 03_service/UwbDistanceManager.kt | BleService, AlertStateMachine |
| AlertStateMachine + Effects | 03_service/AlertStateMachine.kt | BleService, 신규 JVM 테스트 |
| JVM 판정 테스트 | test/.../ble/AlertStateMachineJvmTest.kt | REFACTOR-01 수용 증거 |

</artifacts_this_phase_produces>

<tasks>

<task type="execute" gate="REFACTOR-04">
<name>T1 — CalibrationEngine 추출 (REFACTOR-03)</name>

<read_first>
BleService.kt :100~210 (companion 에코 블록), :725~840 (에코 인스턴스 함수), BleSettingsActivity.kt :375~405
</read_first>

<what-built>
object CalibrationEngine 신설. 코드베이스 관례(DevSettings·BeaconRegistry·UwbCalibrator)를 따라 init(context) 로 prefs 를 잡는다 — 에코 상태는 이미 companion 정적이므로 object 가 정직한 형태다.

이관 대상:
- 상태: echoDiffLive, class EchoDiffStats
- 순수 함수: parseEchoBlob, serializeEchoBlob, echoQuantileDb, echoCalLocalDb, echoCalPriorDb, echoCalAppliedDb
- I/O: echoPrefs, persistEchoAll, persistEchoEntry, loadEchoPriors, maybeUploadEchoCalib
- 기록: recordEchoDiff
- 역할쌍 조회: uwbPairKeyFor (:268) — CalibrationEngine 이 UwbCalibrator 오프셋 조회의 단일 창구가 된다

BleService 잔류: pushRssiEcho (:732, BLE 광고 갱신 경로라 서비스 소유). 호출부는 CalibrationEngine.xxx 로 치환.
BleSettingsActivity 5곳을 CalibrationEngine. 으로 치환.

UwbCalibrator.kt 는 열지 않는다.
</what-built>

<how-to-verify>
`./gradlew testDebugUnitTest` — 42건 통과, 기대값 배열 diff 0.
`git diff --stat` 에 UwbCalibrator.kt / UwbRanger.kt 미등장.
`grep -rn 'BleService\.echo' app/src` → 0건.
</how-to-verify>

<resume-signal>
42건 통과 + 에코 심볼이 BleService.kt 에서 사라짐. 사용자에게 보고 후 T2 착수.
</resume-signal>

<action>
1. CalibrationEngine.kt 작성 (본문은 원본 복사, 수신자만 조정)
2. BleService 원본 블록 삭제 + 호출부 치환
3. BleSettingsActivity 5곳 치환
4. 초기화 진입점 1줄 배선
5. 게이트 실행
</action>
</task>

<task type="execute" gate="REFACTOR-04">
<name>T2 — UwbDistanceManager 추출 (REFACTOR-02)</name>

<read_first>
BleService.kt :685~695 (UWB 필드), :2585~2630 (게이트·신선도·콜백), :3605~3615 (UwbRanger 배선)
</read_first>

<what-built>
class UwbDistanceManager(private val ranger: () -> UwbRanger?) 신설.

이관 대상:
- 필드: peerUwbSeenMap, uwbSampleAtMsMap, uwbSafeStreakMap
- 함수: freshUwbDistM(deviceId), uwbJudgeModeExclusive(deviceId, now)

UwbRanger.uwbDistances 직접 조회는 이 클래스 단독이 된다 (현재 :2590, :2600 두 곳).
BleService 는 private val uwbDist = UwbDistanceManager { uwbRanger } 를 보유하고, 8개 호출처(1101, 1603, 1942, 1992, 2032, 3156, 3392, 3409)를 uwbDist.freshUwbDistM(...) 로 치환한다.

onUwbSampleReceived(:2608)와 judgeUwbOnly(:2628~2853)는 판정이므로 BleService 에 남긴다 → T3 에서 AlertStateMachine 으로 간다. UwbRanger 콜백 배선(:3610)도 서비스 소유로 잔류.

UwbRanger.kt 는 열지 않는다.
</what-built>

<how-to-verify>
`./gradlew testDebugUnitTest` — 42건 통과 (특히 UwbSessionGoldenTest).
`grep -n 'uwbDistances' app/src/main/java/com/wf11/safealert/03_service/BleService.kt` → 주석 외 0건.
`git diff --stat` 에 UwbRanger.kt 미등장.
</how-to-verify>

<resume-signal>
42건 통과 + BleService 에서 uwbDistances 직접 조회 소멸. 사용자 보고 후 T3 착수.
</resume-signal>

<action>
1. UwbDistanceManager.kt 작성
2. BleService 필드·함수 삭제, 8개 호출처 치환
3. 게이트 실행
</action>
</task>

<task type="execute" gate="REFACTOR-04">
<name>T3 — AlertStateMachine 추출 (REFACTOR-01)</name>

<read_first>
BleService.kt :1235~1420 (판정 헬퍼). 이동 직전 `grep -n 'private fun processAlert'` 로 좌표 재확인
</read_first>

<what-built>
class AlertStateMachine(private val fx: Effects, private val uwbDist: UwbDistanceManager) 신설.

Effects 포트 (interface, 정찰에서 확정된 부작용 표면):
- 부작용: vibrateDanger, vibrateWarning, vibrateRapidApproach, stopVibration, sendAlertBroadcast, sendStatusBroadcast, broadcastDeviceList, updateFloatingOverlay, collapseOverlay, forceAlarmVolume, resyncSoundToRemaining, elapsedRealtimeMs
- 조회: extractDisplayName, makeStateLabel, isDeviceMuted, isDwellMuted, updateDwellMute, clearDwellMute, getCurrentMaxLevel, getAudibleMaxLevel, isScreenOn, myCategory

이관 대상:
- processAlert 본문 (:1418~2586)
- 헬퍼 calcLevelWithHysteresis, computePayloadRiskOffset, estimateTTC, updateTrackingState, applyDepartingHysteresis
- judgeUwbOnly (:2628~2853) — 호출처가 onUwbSampleReceived 1곳뿐이므로 판정 단위와 함께 간다
- 경보 경로 전용 상태 맵·상수 (SHADOW_*, TTC_THRESHOLD_SEC, RECEDING_*, RECEDE_*, PEAK_DECAY_ALPHA, APPROACH_TIMEGATE_CORNERING_MS, CLOSING_KMH_TO_DBMS, COLLISION_*, FAST_APPROACH_BYPASS_VEL_DBM, KALMAN_GEOMETRY_MIN_UPDATES)
- 필터 인스턴스 및 판정 경로 전용 상태 맵. alertState 는 UI 폴링이 읽으므로 companion 노출 유지 (AlertStateMachine 이 쓰고 BleService companion 이 가리킨다)

BleService 잔류 (1줄 위임):
processAlert(deviceId, rssi, remoteState, remoteTurn, payloadPresent, peerEchoRssi, nowMs) = asm.processAlert(같은 인자)
→ 리플렉션 진입점·시그니처 보존 = 기존 테스트 6파일과 하네스 무수정. 이것이 동작 무차이의 가장 강한 증거다.

신규 AlertStateMachineJvmTest.kt: Effects 를 기록용 페이크로 구현하고 Robolectric·@RunWith 없이 AlertStateMachine 을 직접 생성해 접근 시퀀스를 1건 재생, SAFE→WARNING→DANGER 전이를 검증.

DevSettings 가 Context 를 요구하는 지점이 남으면 그 값을 임의로 Effects 로 밀어내지 말고, 실패를 보고하고 사용자 판단을 받는다 (설계 변경이므로 임의 진행 금지).
</what-built>

<how-to-verify>
`./gradlew testDebugUnitTest` — 43건(기존 42 + 신규 1) 통과.
`git diff --stat app/src/test` → 신규 파일 1건만, 기존 6파일 변경 0줄.
AlertStateMachineJvmTest 가 org.robolectric 을 import 하지 않음.
BleService.kt 줄 수가 3,934 에서 유의미하게 감소.
</how-to-verify>

<resume-signal>
43건 통과 + 기존 테스트 diff 0줄. Phase 3 코드 작업 종료, SUMMARY 작성 대기.
</resume-signal>

<action>
1. Effects 인터페이스 + AlertStateMachine 골격 작성
2. 본문 이동 (수신자 치환만)
3. BleService 위임 1줄 + Effects 구현
4. 게이트 실행 (42건)
5. AlertStateMachineJvmTest 신설 후 재실행 (43건)
</action>
</task>

</tasks>

## Trust Boundaries

| 경계 | 입력 | 신뢰 | 검증 |
|---|---|---|---|
| BLE 스캔 콜백 → processAlert | 원격 기기 RSSI·1바이트 페이로드 | 불신 | 기존 decodePayload runCatching 유지 (이동만) |
| CalibrationEngine ← SharedPreferences | 저장된 에코 blob | 반신뢰 | 기존 parseEchoBlob 방어 파싱 유지 |
| CalibrationEngine ← Firebase | 원격 prior | 불신 | 기존 경로 유지, 이번 분해에서 변경 없음 |
| AlertStateMachine → Effects | 부작용 호출 | 신뢰 | 테스트에서 페이크로 대체 |

이번 플랜은 신뢰 경계를 이동시키지 않는다. 새 입력·새 외부 통신 0건.

## STRIDE Threat Register

| 위협 | 해당 | 판정 |
|---|---|---|
| Spoofing | 위조 BLE 페이로드로 오경보 유도 | 기존 리스크 그대로 — 분해로 증감 없음 |
| Tampering | 에코 보정 prefs 변조 | 기존과 동일. 단일 소유자화로 접근면 축소 |
| Repudiation | 해당 없음 | 로그 경로 불변 |
| Information disclosure | Firebase 평문 경보 로그 | 기존 이슈, Phase 범위 밖 |
| Denial of service | 판정 경로 예외로 스캔 루프 정지 | 이번 플랜의 실질 위험 — 이동 중 NPE·초기화 순서 오류. 게이트 42건이 방어선 |
| Elevation of privilege | 해당 없음 | 권한 요청 변경 0건 |

주 위험은 보안이 아니라 회귀다. 완화책은 단일: 각 태스크마다 기존 테스트 전량 재실행, 기대값 무수정.
