---
phase: 01-ci
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/src/test/java/com/wf11/safealert/ble/RssiCascadeTest.kt
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [CI-01, CI-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "골든 테스트가 깨지면 `./gradlew testDebugUnitTest` 가 실제로 non-zero 로 종료한다 — 통과만 확인한 게이트는 차단을 증명하지 않는다 (CI-01)"
    - "실패 시 콘솔 한 줄만으로 어느 시나리오·시작상태·프레임·단계가 깨졌는지 특정되고, 리포트 HTML·JUnit XML 이 실제 경로에 생성된다 (CI-02 / D-17 / D-19)"
    - "이 Phase 의 프로덕션 diff 가 KalmanFilter 시임 1건뿐임이 사람 눈으로 확인되었다 — v1.1.70 APK 동작 불변의 근거 (D-03 / D-04)"
    - "실제 태그 push 에서 게이트가 job 안에서 올바른 순서로 실행되고, 아티팩트가 GitHub Actions 실행 페이지에서 내려받아진다 (CI-01 / CI-02 종단 실증)"
    - "테스트 실패로 태그가 차단됐을 때의 복구 절차(수동 태그 삭제 후 재push)가 문서화되어 있고, CI 는 원격 태그를 파괴적으로 건드리지 않는다 (D-16)"
    - "D-21 의 문서 정정 3건이 실제로 반영된 상태다"
  artifacts:
    - .planning/phases/01-ci/01-02-SUMMARY.md
    - .planning/ROADMAP.md
  key_links:
    - "골든 기대값 1개 변조 ↔ Gradle non-zero exit ↔ job 중단 ↔ Build debug APK 미실행 (CI-01 의 실제 인과 사슬)"
    - "`if: always()` ↔ job 이 죽은 실행에서도 아티팩트 탭에 `unit-test-reports` 존재 (CI-02 성립의 필수 링크)"
    - "차단된 태그 ↔ `git tag -d` + `git push --delete origin` 수동 복구 ↔ CI 는 태그를 지우지 않음 (D-16)"
    - "KalmanFilter diff ↔ 호출부 0곳 변경 ↔ APK 동작 불변 주장 (D-04 의 유일한 근거 — 실기 스모크는 ADB 기기 0대라 불가능)"
  prohibitions:
    - id: P-07
      rule: "CI 워크플로에 원격 태그를 삭제·이동하는 스텝을 추가하지 않는다 — 실패한 태그의 정리는 사람의 수동 조작으로만 한다 (D-16). 자동 태그 삭제는 릴리즈 이력을 조용히 재작성한다"
      status: flagged-unverified
    - id: P-08
      rule: "레드 트라이얼로 변조한 기대값을 커밋하지 않는다. 변조 → 확인 → 즉시 원복이 한 작업 단위이며, 원복 확인 없이 다음 단계로 넘어가지 않는다"
      status: flagged-unverified
    - id: P-09
      rule: "D-04 의 '동작 불변'을 실기기 스모크 테스트로 대체하려 시도하지 않는다 — 현재 ADB 연결 기기 0대이며, 검증 불가를 검증한 것처럼 기록하는 것이 이 Phase 의 가장 나쁜 실패 모드다"
      status: flagged-unverified
  assumptions:
    - id: A-CI-01-BLOCK
      status: unresolved
      note: "CI-01 '빌드 차단'의 명세 분류가 `unclassified` 로 반환됨. 본 계획은 이를 '태그 트리거 release job 이 Build debug APK 도달 전에 종료되어 APK·GitHub Release·Firebase 갱신이 모두 일어나지 않는 상태'로 해석하고 Task 3 에서 그 해석을 실증한다. 자동 검증되지 않은 가정이다."
    - id: A-CI-02-DIAG
      status: unresolved
      note: "CI-02 '실기 없이 판별'의 명세 분류가 `unclassified`. 본 계획은 판별 성립 기준을 'Actions 콘솔 로그 + 다운로드한 HTML/XML 리포트만으로 깨진 시나리오·시작상태·프레임·단계를 지목할 수 있음'으로 해석했다. 자동 검증되지 않은 가정이다."
    - id: A-TAG-TRIGGER
      status: unresolved
      note: "Task 3 의 종단 실증은 실제 `v*` 태그 push 를 요구한다. 이 저장소의 태그 push 는 GitHub Release 생성까지 이어지므로, 검증용 태그를 쓸지 실제 다음 릴리즈 태그에 얹을지는 사용자 판단 사항이다 — 체크포인트에서 사용자에게 명시적으로 묻는다."
---

<objective>
01-01 이 만든 게이트가 **실제로 차단하는지** 그리고 **실기 없이 진단 가능한지** 를 증명하고, 이 Phase 의 프로덕션 변경이 시임 1건뿐임을 사람 눈으로 확정한다.

Purpose: 01-01 의 검증은 전부 "초록"만 확인한다. 초록은 게이트가 존재한다는 증거지 게이트가 막는다는 증거가 아니다 — 실패 경로를 한 번도 밟아 보지 않은 CI 게이트는 몇 달 뒤 아무것도 막지 않고 있었다는 사실이 밝혀지는 전형적 실패다. CI-01 과 CI-02 는 빨간 실행을 한 번 통과시켜야 비로소 충족된다.

Output: 로컬 레드/그린 트라이얼 기록, 프로덕션 diff 감사 결과, 실제 태그 push 기반 종단 실증, 차단된 태그 복구 절차(D-16) 기록.

이 계획은 코드를 거의 만들지 않는다. 만들어진 것이 주장한 대로 동작하는지 확인하고, 확인되지 않은 것을 확인된 것처럼 넘기지 않는 것이 전부다.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-ci/01-CONTEXT.md
@.planning/phases/01-ci/01-01-SUMMARY.md

@.github/workflows/release.yml
@app/src/test/java/com/wf11/safealert/ble/RssiCascadeTest.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: 레드 트라이얼 — 게이트가 실제로 막는지 실패 경로로 증명</name>

  <files>app/src/test/java/com/wf11/safealert/ble/RssiCascadeTest.kt</files>

  <read_first>
app/src/test/java/com/wf11/safealert/ble/RssiCascadeTest.kt
.github/workflows/release.yml
.planning/phases/01-ci/01-01-SUMMARY.md
.planning/REQUIREMENTS.md
  </read_first>

  <precondition>
작업 트리가 깨끗해야 한다 (`git status --porcelain` 출력 없음). 변조 → 원복 절차가 다른 미커밋 변경과 섞이면 원복 완결을 확인할 수 없다.
  </precondition>

  <action>
게이트의 실패 경로를 실제로 한 번 밟는다. 세 단계 모두 로컬에서 수행하며 어떤 결과물도 커밋하지 않는다.

**(1) 그린 기준선 확보**

`./gradlew testDebugUnitTest --no-daemon --console=plain` 을 실행해 exit 0 을 확인하고, 이때 실제로 생성된 두 경로가 존재하는지 파일시스템에서 직접 확인한다: `app/build/reports/tests/testDebugUnitTest/index.html` 과 `app/build/test-results/testDebugUnitTest/` 아래의 `TEST-*.xml`. 이 두 경로가 실존해야 `release.yml` 의 업로드 스텝이 빈 아티팩트를 올리지 않는다(D-17). 경로가 다르면 게이트는 초록인데 CI-02 는 조용히 미충족이 된다 — 여기서 잡지 않으면 실제 태그 push 에서야 발견된다.

**(2) 레드 트라이얼 (CI-01 실증)**

`RssiCascadeTest.kt` 의 기대값 배열 중 하나를 골라 원소 **하나만** 최소 단위로 어긋나게 만든다. 두 곳을 고른다:
- `EXPECTED_*_KALMAN` (DoubleArray) 의 중간 인덱스 값을 `1e-6` 만큼 이동 — 1e-9 허용오차(D-08)가 실제로 이 크기의 차이를 잡아내는지 확인하는 목적을 겸한다. 잡히지 않으면 delta 인자가 빠졌거나 잘못된 오버로드를 쓰고 있다는 뜻이다(P-04).
- `EXPECTED_*_MEDIAN` (IntArray) 의 중간 인덱스 값을 `+1` — Int 정확 일치 경로 확인.

각 변조마다 `./gradlew testDebugUnitTest --no-daemon --console=plain` 을 실행하고 다음 셋을 모두 기록한다:
- 종료 코드가 non-zero 인가 (CI-01 의 차단 메커니즘 그 자체 — GitHub Actions 는 스텝의 non-zero 종료로만 job 을 죽인다)
- 콘솔 출력에 `<시나리오>/<시작상태> frame=<i> stage=<단계>` 4요소가 전부 찍히는가 (D-19 / CI-02)
- `app/build/reports/tests/testDebugUnitTest/index.html` 이 실패 상태로 **여전히 생성되는가** (실패 시 리포트가 안 나오면 `if: always()` 를 달아도 올릴 것이 없다)

기록한 콘솔 실패 라인 원문은 SUMMARY 에 그대로 붙여 둔다 — 이것이 CI-02 충족의 1차 증거다.

**(3) 원복 확인 (P-08)**

`git checkout -- app/src/test/java/com/wf11/safealert/ble/RssiCascadeTest.kt` 로 되돌리고 `git status --porcelain` 이 빈 출력인지, `./gradlew testDebugUnitTest` 가 다시 exit 0 인지 확인한다. 변조본이 커밋에 섞이면 골든이 처음부터 틀린 값을 지키게 된다.

**(4) D-21 문서 정정 재확인**

D-21 은 문서 정정 3건이며 이미 반영된 것으로 보고되었다. 실제로 그런지 확인만 한다 — 일치하면 아무것도 고치지 않는다:
- `.planning/REQUIREMENTS.md` 의 TEST-03 항목이 캐스케이드 순서를 `MedianFilter(3샘플) → RssiPreFilter → KalmanFilter` 로 기술하는가 (윈도우 크기·순서 오기 정정)
- `.planning/ROADMAP.md` 의 Phase 1 출하 상태 기술과 성공 기준 2번 항목이 CONTEXT.md D-21 이 지시한 문구와 일치하는가

불일치가 발견될 때만 해당 줄을 범위 한정 편집으로 고친다. 파일 전체를 다시 쓰지 않는다.
  </action>

  <acceptance_criteria>
    - 그린 기준선: `./gradlew testDebugUnitTest --no-daemon --console=plain` exit 0
    - `app/build/reports/tests/testDebugUnitTest/index.html` 이 존재한다
    - `app/build/test-results/testDebugUnitTest/` 아래에 `TEST-*.xml` 이 1개 이상 존재한다
    - Kalman 기대값 `1e-6` 변조 시 exit 코드가 non-zero 이고, 실패 출력에 `frame=` 와 `stage=kalman` 이 함께 나타난다
    - Median 기대값 `+1` 변조 시 exit 코드가 non-zero 이고, 실패 출력에 `stage=median` 이 나타난다
    - 실패 실행 이후에도 `app/build/reports/tests/testDebugUnitTest/index.html` 이 존재한다
    - 원복 후 `git status --porcelain` 이 빈 출력이고 `./gradlew testDebugUnitTest` 가 다시 exit 0
    - `grep -c 'MedianFilter(3샘플) → RssiPreFilter → KalmanFilter' .planning/REQUIREMENTS.md` 가 1 (D-21)
    - SUMMARY 에 레드 트라이얼 2건의 콘솔 실패 라인 원문이 기록되어 있다
  </acceptance_criteria>

  <verify>
    <automated>./gradlew testDebugUnitTest --no-daemon --console=plain && git status --porcelain</automated>
  </verify>

  <done>
기대값 변조 2건이 모두 non-zero 종료를 유발했고 실패 메시지가 D-19 4요소를 모두 담았으며, 실패 실행에서도 리포트 산출물이 생성되고, 변조는 전부 원복되어 작업 트리가 깨끗하다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: 프로덕션 diff 감사 — "APK 동작 불변" 확정 (D-04 / D-03)</name>

  <files>app/src/main/java/com/wf11/safealert/02_ble/KalmanFilter.kt</files>

  <read_first>
app/src/main/java/com/wf11/safealert/02_ble/KalmanFilter.kt
.planning/phases/01-ci/01-01-SUMMARY.md
  </read_first>

  <what-built>
01-01 에서 프로덕션 코드에 들어간 변경은 `KalmanFilter` 생성자의 시간 시임 1건뿐이다(D-01): `nowMs: () -> Long = { System.currentTimeMillis() }` 기본 인자 추가 + `update()`·`injectWarmup()` 내부의 `System.currentTimeMillis()` 직접 호출을 그 시임 호출로 교체. 기본 인자이므로 호출부 변경 0건이며, 테스트·CI 산출물(`RssiCascadeTest.kt`, `RssiCascadeIsolationTest.kt`, `app/build.gradle` testOptions, `release.yml` 2개 스텝)은 APK 런타임 동작에 관여하지 않는다.

Claude 가 이 체크포인트 직전에 준비해 둘 것:
- `git diff <phase 시작 커밋>..HEAD -- app/src/main/` 전체 출력(짧아야 정상)
- `git diff --stat <phase 시작 커밋>..HEAD -- app/src/main/` — 파일 1개만 나와야 함
- `git diff <phase 시작 커밋>..HEAD --name-only -- app/src/main/` — `KalmanFilter.kt` 외 항목 없음
  </what-built>

  <how-to-verify>
D-04 에 따라 이 Phase 의 "v1.1.70 APK 동작 불변" 판정 근거는 **실기 스모크가 아니라 이 diff 검토뿐**이다. 현재 연결된 ADB 기기가 0대라 실기 확인 경로 자체가 없다(P-09 — 실기 스모크로 D-04 를 대체하지 말 것).

위에 제시된 diff 를 보고 4개 항목을 확인해 주십시오:
1. 변경된 프로덕션 파일이 `KalmanFilter.kt` 하나뿐입니까?
2. 변경 내용이 생성자에 `nowMs: () -> Long = { System.currentTimeMillis() }` 기본 인자를 추가하고, `update()` 와 `injectWarmup()` 이 그 시임을 호출하도록 바꾼 것뿐입니까?
3. 수치 상수(`pRR=5.0`/`pVV=5.0` 콜드스타트, `pRR=25.0`/`pVV=5.0` 워밍업, `coerceIn(0.05, 2.0)`, processNoise/measureNoise 프리셋 값), `updateCnt` 증가 위치, `reset()`, `updatePreset()`, 공개 게터가 전부 그대로입니까? (D-03)
4. `KalmanFilter` 를 호출하는 코드가 **한 곳도** 바뀌지 않았습니까? (기본 인자이므로 바뀔 이유가 없습니다 — 바뀌었다면 D-03 위반입니다)
  </how-to-verify>

  <resume-signal>
"승인" 이라고 입력하면 Task 3 의 태그 push 실증으로 진행합니다. 어긋난 항목이 있으면 항목 번호와 내용을 알려 주십시오 — 01-01 로 되돌아가 시임 범위를 다시 좁힙니다.

이 체크포인트는 `gate="blocking-human"` 이므로 auto 모드에서도 자동 승인되지 않습니다. 이 Phase 에서 사람 눈이 유일한 검증 수단인 지점이기 때문입니다.
  </resume-signal>

  <action>
사용자에게 제시할 것을 먼저 준비한다:
- `git diff <phase 시작 커밋>..HEAD -- app/src/main/` 의 전체 출력 (짧아야 정상이다)
- `git diff --stat <phase 시작 커밋>..HEAD -- app/src/main/` — 파일 1개만 나와야 한다
- `KalmanFilter` 를 참조하는 호출부 목록과 그 파일들의 diff 가 비어 있음을 보이는 확인 (`git diff <phase 시작 커밋>..HEAD --name-only -- app/src/main/` 결과에 `KalmanFilter.kt` 외 항목 없음)

그 다음 사용자에게 확인을 요청한다.

**확인 요청 문구:**

Phase 1 의 프로덕션 변경은 아래 diff 가 전부입니다. D-04 에 따라 이 Phase 의 "v1.1.70 APK 동작 불변"은 **실기 스모크가 아니라 이 diff 검토로만** 판정합니다 — 현재 연결된 ADB 기기가 0대이므로 실기 확인 경로 자체가 없습니다(P-09).

검토해 주실 항목:
1. 변경된 프로덕션 파일이 `KalmanFilter.kt` 하나뿐입니까?
2. 변경 내용이 생성자에 `nowMs: () -> Long = { System.currentTimeMillis() }` 기본 인자를 추가하고, `update()` 와 `injectWarmup()` 이 그 시임을 호출하도록 바꾼 것뿐입니까?
3. 수치 상수(`pRR=5.0`/`pVV=5.0` 콜드스타트, `pRR=25.0`/`pVV=5.0` 워밍업, `coerceIn(0.05, 2.0)`, processNoise/measureNoise 프리셋 값), `updateCnt` 증가 위치, `reset()`, `updatePreset()`, 공개 게터가 전부 그대로입니까? (D-03)
4. `KalmanFilter` 를 호출하는 코드가 **한 곳도** 바뀌지 않았습니까? (기본 인자이므로 바뀔 이유가 없습니다 — 바뀌었다면 D-03 위반입니다)

**승인 시:** Task 3 의 태그 push 실증으로 진행합니다.
**거부 시:** 어느 항목이 어긋났는지 알려 주시면 01-01 로 되돌아가 시임 범위를 다시 좁힙니다.

이 체크포인트는 자동 승인 대상이 아닙니다. 이 Phase 에서 사람 눈이 유일한 검증 수단인 지점이기 때문입니다.
  </action>

  <acceptance_criteria>
    - `git diff --stat` 이 `app/src/main/` 아래 파일 1개(`KalmanFilter.kt`)만 보고한다
    - 사용자가 위 4개 항목에 대해 명시적으로 승인했다
    - 승인 또는 거부 사유가 SUMMARY 에 기록되었다
  </acceptance_criteria>

  <verify>
    <human-check>사용자가 프로덕션 diff 4개 항목을 검토하고 "시임 1건뿐"임을 승인</human-check>
  </verify>

  <done>
사용자가 프로덕션 diff 가 KalmanFilter 시임 1건뿐임을 확인·승인했고, 그 판정 근거가 diff 검토임이(실기 스모크 아님) SUMMARY 에 명시되었다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: 실제 태그 push 로 CI-01 / CI-02 종단 실증</name>

  <files>.github/workflows/release.yml</files>

  <read_first>
.github/workflows/release.yml
.planning/phases/01-ci/01-01-SUMMARY.md
  </read_first>

  <precondition>
`origin` 이 GitHub 저장소를 가리키고 태그 push 권한이 있어야 하며, 저장소 시크릿 `GOOGLE_SERVICES_JSON` 과 `DEBUG_KEYSTORE` 가 설정되어 있어야 한다 (`release.yml` 의 복원 스텝이 이들에 의존한다). 확인: `git remote -v` 와 GitHub 저장소 Settings → Secrets 화면.
  </precondition>

  <what-built>
01-01 이 `release.yml` 의 `Grant execute permission` (`chmod +x gradlew`) 뒤·`Extract version` 앞에 두 스텝을 넣었다(D-13 / D-14): `Run unit tests (golden RSSI cascade)` = `./gradlew testDebugUnitTest --no-daemon --console=plain` (D-15), 그리고 그 뒤에 `if: always()` 로 도는 `actions/upload-artifact@v7` 스텝이 `app/build/reports/tests/testDebugUnitTest/` 와 `app/build/test-results/testDebugUnitTest/` 두 디렉터리를 `unit-test-reports` 라는 이름으로 올린다(D-17 / D-18).

Task 1 의 로컬 레드 트라이얼은 Gradle 이 non-zero 로 죽는다는 것까지만 증명했다. 그것이 **GitHub Actions job 안에서 올바른 위치에 있어 APK 빌드를 실제로 막는지**, 그리고 **아티팩트가 실제로 다운로드 가능한지**는 실행을 한 번 돌려야만 알 수 있다. 이 체크포인트가 그 실행을 요청한다.
  </what-built>

  <how-to-verify>
**먼저 정해 주실 것 — 어느 태그로 실증할지 (A-TAG-TRIGGER):**

이 저장소의 `v*` 태그 push 는 GitHub Release 생성까지 이어집니다. 두 선택지가 있습니다.
- (a) 다음 실제 릴리즈 태그에 얹어 검증한다 — 별도 태그를 만들지 않지만, 실패 시 릴리즈가 지연됩니다.
- (b) 검증 전용 태그를 한 번 밀고 확인 후 Release 와 태그를 수동 정리한다 — 릴리즈 이력에 임시 항목이 잠깐 남습니다.

CI 는 어느 쪽에서도 원격 태그를 자동으로 지우지 않습니다(D-16 / P-07).

**그린 경로 — CI-01 위치 검증:**
1. Actions 실행 로그에서 스텝 순서가 `Restore google-services.json` → `Grant execute permission` → `Run unit tests (golden RSSI cascade)` → `Extract version` → `Build debug APK` 인지
2. `Run unit tests` 스텝이 초록으로 통과했는지 — configuration 단계 오류가 아니라 실제 테스트가 돌았는지 확인하십시오. 로그에 테스트 실행 흔적이 보여야 합니다. google-services 복원보다 앞에 뒀다면 여기서 테스트 실패가 아닌 Gradle 설정 오류가 났을 것입니다
3. 실행 페이지 Artifacts 섹션에 `unit-test-reports` 가 있고, 내려받아 압축을 풀면 `index.html` 이 브라우저에서 열리는지 (CI-02)

**레드 경로 — CI-01 차단 실증 (이 Phase 의 핵심 증거):**

위 그린 실행이 확인되면, 기대값 하나를 어긋나게 한 커밋으로 태그를 한 번 더 밀어 다음을 확인합니다.
4. `Run unit tests` 스텝이 빨간색이고 job 이 거기서 멈췄는지
5. `Build debug APK`·`Rename APK`·`Create GitHub Release & Upload APK` 세 스텝이 **실행되지 않았고**, GitHub Release 가 생성되지 않았으며, Firebase 버전 정보도 갱신되지 않았는지 (CI-01)
6. **실패한 실행인데도** Artifacts 에 `unit-test-reports` 가 여전히 존재하는지 (`if: always()` — D-18. 이게 없으면 정작 필요한 순간에 진단 자료가 없습니다)
7. 다운로드한 리포트만 보고 어느 시나리오·시작상태·프레임·단계가 깨졌는지 실기 없이 지목되는지 (CI-02 / D-19)

**차단된 태그 복구 절차 (D-16):**

테스트 실패로 태그가 차단되면 CI 는 아무것도 정리하지 않습니다. 사람이 수동으로 처리합니다: 로컬 태그 삭제(`git tag -d v<버전>`) → 원격 태그 삭제(`git push --delete origin v<버전>`) → 원인 수정 커밋 → 같은 태그 재생성·재push. 부분 생성된 GitHub Release 가 있으면 Releases 화면에서 함께 지웁니다. CI 워크플로에 원격 태그를 지우는 스텝을 추가하지 않는 이유는 그것이 릴리즈 이력을 조용히 재작성하기 때문입니다(P-07).
  </how-to-verify>

  <resume-signal>
태그 전략 (a) 또는 (b) 를 먼저 알려 주시고, 그린·레드 두 실행을 확인한 뒤 "승인" 이라고 입력하면 Phase 1 이 완료됩니다(CI-01·CI-02 실증 충족). 관측된 증상(스텝 순서 이상 / 아티팩트 부재 / 실패했는데 Build 가 돌았음 등)이 있으면 그대로 알려 주십시오 — 01-01 의 `release.yml` 배선으로 되돌아갑니다.

이 체크포인트는 `gate="blocking-human"` 이므로 auto 모드에서도 자동 승인되지 않습니다. 실제 태그 push 는 릴리즈 파이프라인을 건드리는 되돌리기 비싼 행위이기 때문입니다.
  </resume-signal>

  <action>
로컬 레드 트라이얼은 Gradle 이 non-zero 로 죽는다는 것까지만 증명한다. 그것이 **GitHub Actions job 안에서 올바른 위치에 있어서 APK 빌드를 실제로 막는지**, 그리고 **아티팩트가 실제로 다운로드 가능한지**는 실행을 한 번 돌려야만 알 수 있다. 이 체크포인트가 그 실행을 요청한다.

사용자에게 다음을 제시하고 판단을 요청한다.

**먼저 물을 것 — 어느 태그로 실증할지 (A-TAG-TRIGGER):**

이 저장소의 `v*` 태그 push 는 GitHub Release 생성까지 이어집니다. 두 선택지가 있습니다.
- (a) 다음 실제 릴리즈 태그에 얹어 검증한다 — 별도 태그를 만들지 않지만, 실패 시 릴리즈가 지연됩니다.
- (b) 검증 전용 태그를 한 번 밀고 확인 후 Release 와 태그를 수동 정리한다 — 릴리즈 이력에 임시 항목이 잠깐 남습니다.

어느 쪽을 택하실지 알려 주십시오. CI 는 어느 쪽에서도 원격 태그를 자동으로 지우지 않습니다(D-16 / P-07).

**확인해 주실 것 (그린 경로 — CI-01 위치 검증):**
1. Actions 실행 로그에서 스텝 순서가 `Restore google-services.json` → `Grant execute permission` → `Run unit tests (golden RSSI cascade)` → `Extract version` → `Build debug APK` 인지
2. `Run unit tests` 스텝이 초록으로 통과했는지 (configuration 단계 오류가 아니라 실제 테스트가 돌았는지 — 로그에 테스트 실행 흔적이 보여야 합니다. google-services 복원보다 앞에 뒀다면 여기서 테스트 실패가 아닌 Gradle 설정 오류가 났을 것입니다)
3. 실행 페이지 Artifacts 섹션에 `unit-test-reports` 가 있고, 내려받아 압축을 풀면 `index.html` 이 브라우저에서 열리는지 (CI-02)

**확인해 주실 것 (레드 경로 — CI-01 차단 실증):**

이것이 이 Phase 의 핵심 증거입니다. 위 그린 실행이 확인되면, 기대값 하나를 어긋나게 한 커밋으로 태그를 한 번 더 밀어 다음을 확인합니다.
4. `Run unit tests` 스텝이 빨간색이고 job 이 거기서 멈췄는지
5. `Build debug APK`·`Rename APK`·`Create GitHub Release & Upload APK` 세 스텝이 **실행되지 않았고**, GitHub Release 가 생성되지 않았으며, Firebase 버전 정보도 갱신되지 않았는지 (CI-01)
6. **실패한 실행인데도** Artifacts 에 `unit-test-reports` 가 여전히 존재하는지 (`if: always()` — D-18. 이게 없으면 정작 필요한 순간에 진단 자료가 없습니다)
7. 다운로드한 리포트만 보고 어느 시나리오·시작상태·프레임·단계가 깨졌는지 실기 없이 지목되는지 (CI-02 / D-19)

**차단된 태그 복구 절차 (D-16) — 사용자에게 안내하고 SUMMARY 에 기록:**

테스트 실패로 태그가 차단되면 CI 는 아무것도 정리하지 않습니다. 사람이 수동으로 처리합니다:
로컬 태그 삭제(`git tag -d v<버전>`) → 원격 태그 삭제(`git push --delete origin v<버전>`) → 원인 수정 커밋 → 같은 태그 재생성·재push. 부분 생성된 GitHub Release 가 있으면 Releases 화면에서 함께 지웁니다. CI 워크플로에 원격 태그를 지우는 스텝을 추가하지 않는 이유는 그것이 릴리즈 이력을 조용히 재작성하기 때문입니다(P-07).

**승인 시:** Phase 1 완료. CI-01 과 CI-02 가 실증으로 충족됩니다.
**거부 시:** 관측된 증상(스텝 순서 이상 / 아티팩트 부재 / 실패했는데 Build 가 돌았음 등)을 알려 주시면 01-01 의 `release.yml` 배선으로 되돌아갑니다.
  </action>

  <acceptance_criteria>
    - 사용자가 (a)/(b) 중 태그 전략을 선택했다
    - 그린 실행에서 스텝 순서 3개 경계(google-services 이후 / Build 이전 / Extract version 이전)가 모두 확인되었다
    - 그린 실행 Artifacts 에 `unit-test-reports` 가 존재하고 `index.html` 이 열린다
    - 레드 실행에서 `Run unit tests` 가 실패하고 `Build debug APK` 이하 스텝이 미실행이며 GitHub Release 가 생성되지 않았다
    - 레드 실행에서도 `unit-test-reports` 아티팩트가 존재한다
    - 다운로드한 리포트만으로 깨진 조합이 지목되었고 그 지목 내용이 SUMMARY 에 기록되었다
    - D-16 복구 절차가 SUMMARY 에 기록되었다
    - `grep -c 'tag --delete\|push --delete origin' .github/workflows/release.yml` 가 0 (P-07)
  </acceptance_criteria>

  <verify>
    <human-check>사용자가 실제 태그 push 로 그린·레드 두 실행을 확인하고 CI-01(차단)·CI-02(아티팩트 기반 진단) 충족을 승인</human-check>
  </verify>

  <done>
실제 GitHub Actions 실행에서 게이트가 올바른 위치에서 통과·차단 양쪽을 보였고, 실패 실행에서도 리포트 아티팩트로 원인이 실기 없이 특정되었으며, 차단된 태그의 수동 복구 절차가 기록되었다.
  </done>
</task>

</tasks>

<threat_model>
ASVS Level 1 / block_on: high. 이 계획은 새 코드 표면을 거의 만들지 않으며, 위협 대부분이 "검증하지 않은 것을 검증했다고 기록하는" 무결성 문제(Repudiation/Tampering)에 몰려 있다.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 작업 트리 → 커밋 | 레드 트라이얼의 변조본이 이 경계를 넘으면 골든이 틀린 값을 영구 고정한다 |
| 태그 push → 릴리즈 파이프라인 | 신뢰되지 않은 트리거가 서명 APK 배포·GitHub Release·Firebase 갱신으로 이어지는 지점. 이 계획이 그 경로를 실제로 한 번 통과시킨다 |
| CI 실행 → 검증 기록(SUMMARY) | 관측 결과가 사실로 승격되는 지점. 미관측 항목이 관측된 것처럼 기록되면 이후 모든 Phase 가 거짓 전제 위에 쌓인다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Repudiation | 게이트 충족 판정 | high | mitigate | CI-01 을 초록 실행만으로 충족 처리하지 않는다. Task 1(로컬 레드)과 Task 3(CI 레드) 두 층에서 실패 경로를 실제로 밟고, `Build debug APK` 미실행과 Release 미생성을 눈으로 확인해야 통과다. 실패 경로를 안 밟은 게이트는 몇 달 뒤 "아무것도 막고 있지 않았다"로 드러난다 |
| T-01-08 | Tampering | `RssiCascadeTest.kt` 변조본 | high | mitigate | P-08 — 변조 → 확인 → 즉시 원복이 한 작업 단위. 원복 후 `git status --porcelain` 빈 출력을 수용 기준으로 못박는다. 변조본이 커밋되면 골든이 틀린 값을 지키며 이후 모든 회귀 판정이 무효가 된다 |
| T-01-09 | Repudiation | D-04 동작 불변 주장 | medium | mitigate | P-09 — 실기 스모크 불가(ADB 0대)를 SUMMARY 에 명시하고, 판정 근거가 diff 검토뿐임을 기록한다. 검증 불가를 검증한 것처럼 적는 것이 이 Phase 최악의 실패 모드 |
| T-01-10 | Tampering | 원격 태그·릴리즈 이력 | medium | mitigate | P-07 — CI 에 태그 삭제·이동 스텝을 넣지 않고 수동 복구 절차(D-16)만 문서화한다. 자동 태그 정리는 릴리즈 이력을 조용히 재작성해 사후 감사를 불가능하게 만든다 |
| T-01-11 | Information Disclosure | 검증용 태그가 만든 GitHub Release | low | accept | 태그 전략 (b) 선택 시 임시 Release 가 잠깐 공개된다. 산출물은 이미 공개 배포 대상인 debug APK 와 동일하고 새로운 노출 표면이 아니다. 정리는 사용자가 수동으로 수행 |
| T-01-12 | Denial of Service | 릴리즈 지연 | low | accept | 태그 전략 (a) 선택 시 게이트 실패가 실제 릴리즈를 지연시킨다. 이는 CI-01 의 의도된 동작이며 결함이 아니다. 복구 경로는 D-16 에 문서화됨 |
| T-01-SC | Tampering | npm/pip/cargo 설치 | n/a | accept | 이 계획은 패키지를 추가하지 않는다. 코드 변경이 사실상 없으며 검증·문서 작업만 수행한다. RESEARCH.md Package Legitimacy Audit N/A — `[ASSUMED]`/`[SUS]` 사람 확인 체크포인트 불필요 |

High 등급 2건(T-01-07, T-01-08) 모두 disposition `mitigate` 이며 각각 수용 기준으로 강제된다. `block_on: high` 기준에서 미조치 High 항목은 없다.
</threat_model>

<verification>
1. 로컬 레드 트라이얼 2건이 non-zero 종료 + D-19 4요소 메시지 + 실패 시 리포트 생성을 모두 만족.
2. 원복 후 `git status --porcelain` 빈 출력, `./gradlew testDebugUnitTest` exit 0.
3. `git diff --stat` 상 프로덕션 변경 파일 1개, 사용자 승인 완료.
4. GitHub Actions 그린 실행: 스텝 순서 정상, 아티팩트 다운로드·열람 가능.
5. GitHub Actions 레드 실행: `Run unit tests` 실패, `Build debug APK` 이하 미실행, Release 미생성, 아티팩트는 존재.
6. `release.yml` 에 원격 태그 파괴 스텝 부재.
7. SUMMARY 에 실기 스모크 미수행 사실과 그 사유(ADB 0대)가 명시됨.
</verification>

<success_criteria>
- 골든 테스트 실패가 실제로 `./gradlew testDebugUnitTest` non-zero 종료를 유발하고, GitHub Actions job 을 `Build debug APK` 이전에 중단시킨다 (CI-01 실증 완료)
- 실패 실행의 아티팩트만으로 깨진 시나리오·시작상태·프레임·단계가 실기 없이 지목된다 (CI-02 실증 완료)
- 프로덕션 diff 가 KalmanFilter 시임 1건뿐임이 사용자 승인으로 확정되었다 (D-03 / D-04)
- 차단된 태그의 수동 복구 절차가 기록되었고 CI 는 원격 태그를 자동으로 건드리지 않는다 (D-16)
- D-21 문서 정정 3건 반영이 재확인되었다
</success_criteria>

## Artifacts this phase produces

이 계획은 새 코드 심볼을 만들지 않는다. 산출물은 검증 기록과 문서다.

**신규 파일**
- `.planning/phases/01-ci/01-02-SUMMARY.md`

**SUMMARY 에 기록되는 증거 항목 (이 계획의 실질 산출물)**
- 로컬 레드 트라이얼 2건의 콘솔 실패 라인 원문 (Kalman 1e-6 변조 / Median +1 변조)
- 그린·레드 GitHub Actions 실행 URL 2건
- 레드 실행에서 미실행된 스텝 목록 (`Build debug APK`, `Rename APK`, `Create GitHub Release & Upload APK`)
- 아티팩트 `unit-test-reports` 의 그린·레드 양쪽 존재 확인 기록
- 다운로드 리포트만으로 지목한 "깨진 조합" 문장 1건 (CI-02 충족의 직접 증거)
- D-16 태그 수동 복구 절차 (로컬 태그 삭제 → 원격 태그 삭제 → 수정 커밋 → 재태그·재push → 부분 Release 수동 삭제)
- D-04 판정 근거 명시: 실기 스모크 미수행, 사유 = ADB 연결 기기 0대, 판정 수단 = 프로덕션 diff 검토

**변경 가능 문서 (불일치 시에만)**
- `.planning/REQUIREMENTS.md` — TEST-03 캐스케이드 기술 (D-21, 이미 반영된 것으로 확인됨)
- `.planning/ROADMAP.md` — Phase 1 출하 상태·성공 기준 2번 (D-21)

**변경 없는 상태로 확정되는 것**
- `.github/workflows/release.yml` — 원격 태그를 삭제·이동하는 스텝은 끝까지 추가되지 않는다 (P-07)
- `app/src/test/java/com/wf11/safealert/ble/RssiCascadeTest.kt` — 레드 트라이얼 변조는 전부 원복되어 net diff 0 (P-08)

<output>
Create `.planning/phases/01-ci/01-02-SUMMARY.md` when done
</output>
