---
quick_id: 260912-32e
slug: safealert-v1-1-88-rules-lock-whats-new-r
date: 2026-09-12
---

# v1.1.88 — 규칙 잠금 + 첫 실행 변경 사항 창 + 설치 창 수신 수정

사용자 지시(2026-09-12): 테스트 단계라 C4(전 기기 1.1.87) 선행조건 면제. Phase D 는
260911-k7r-PLAN.md 181-191행 그대로 적용 (재계획 없음). 커밋·태그·푸시 금지.

## Tasks

1. database.rules.json — Phase D 그대로
   - beacon_share/$key, echo_calib/$id (site 아래 + 최상위) `.write` true -> "auth != null"
   - alerts, uwb_probe "!data.exists()" -> "auth != null && !data.exists()"
   - beacon_share, echo_calib `.read` true -> "auth != null"
   - version 두 곳 `.read` true 유지
2. app/build.gradle — versionCode 144 / versionName 1.1.88
3. MainActivity.onCreate — safealert_prefs `last_seen_version_code` < BuildConfig.VERSION_CODE 이면
   AlertDialog 1회 표시 후 저장 (새 설치 포함). 본문 = strings.xml `whats_new`.
4. UpdateManager.kt registerReceiver 플래그 RECEIVER_NOT_EXPORTED -> RECEIVER_EXPORTED
   (ACTION_DOWNLOAD_COMPLETE 는 DownloadProvider 가 보내며 system uid 가 아님. 수신기는 id 비교 +
   SHA-256 일치 후에만 설치하므로 위조 방송으로 설치되지 않음)

## Verify

- 로컬: assembleDebug 성공, rules JSON 파싱 성공
- 태그 후: V-1 (무인증 PUT /TESTSITE/beacon_share/_probe.json 401, /version.json 200), V-2 전체,
  87 -> 88 자동 업데이트에서 다운로드 완료 후 설치 창 표시 확인
